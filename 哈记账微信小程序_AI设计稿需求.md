# 哈记账微信小程序｜AI 设计稿生成需求

> 版本：v1.1 · 设计交付基线
>
> 本稿是给 AI 设计工具、产品和前端共同使用的单一约束文档。凡标记为“首版必须”的内容必须产出高保真页面或明确状态；凡标记为“预留/暂不开放”的内容只允许出现信息架构说明，不得生成可点击的已上线入口。

## 0. 设计交付摘要（先读这一节）

- 品牌：哈记账；使用目录中的 `哈记账.png` 作为品牌标识参考，图标只做品牌识别，不承担金额或业务状态表达。
- 核心承诺：“3 秒记下一笔，10 秒看懂当月”。首屏优先看懂本月支出，再完成记账。
- 首版只有 4 个底部导航：首页、日历、资产、我的。统计不进入底部导航，只保留低保真路由占位。
- 记账主流程必须是全屏页面；分类、账户、日期、备注使用靠近触发控件的选择器或短任务弹窗；不把表单塞进窄弹层。
- 三种账单类型必须清楚区分：支出、收入、转账。转账必须有转出账户和转入账户，且两者不能相同。
- 默认页面展示真实可读的示例数据；新增记账默认金额为空，凭证默认为空，不要用“上传失败”等异常状态冒充默认态。
- 凭证只是可选附件，不做 OCR、金额识别和自动入账；文件链路由 Java 后端鉴权，MinIO 不暴露给小程序。
- 每个核心页面至少交付：默认、空、加载、错误和关键操作态；每条账单可进入详情编辑，每个账户可新增和编辑。

## 1. 项目概述

产品名称：哈记账。

请设计一款名为“哈记账”的移动端优先微信记账小程序，帮助用户快速记录日常收入与支出，并查看账单、日历、统计和个人设置。

产品定位：哈记账是一款清新、柔和、自然、亲和的生活工具类产品；以“3 秒记下一笔，10 秒看懂当月”为核心体验目标。

目标用户：希望轻松记录日常消费、查看收支趋势、培养记账习惯的普通用户。

核心目标：

- 让用户可以快速完成一条记账。
- 让用户一眼看懂本月收入、支出和结余。
- 让用户方便地按日期、分类和账本查看历史账单。
- 通过清晰的统计图表帮助用户了解消费结构。

本稿的第一版范围：单用户、人民币单币种、手动记账、账单查询、基础账户管理、周期记账和基础文件服务。文件服务首版用于用户头像和可选的账单凭证图片；不做票据 OCR 和自动识别。暂不包含多人共享账本、银行卡/支付平台自动同步、预算、投资收益、复杂报表和多币种换算。AI 生成设计稿不得自行扩展这些未纳入范围的功能。

## 2. 技术与实现边界

- 产品形态：微信小程序。
- 后端：Java。
- 前端建议：Taro + React + TypeScript，或等价的微信小程序跨端方案。
- 文件服务：部署 MinIO，使用 S3 兼容 API 作为对象存储；文件元数据由 Java 后端管理，业务页面不得直接依赖 MinIO 内部地址或暴露 Access Key/Secret Key。
- 文件访问：默认使用 Java 后端签发的短时效预签名上传/下载 URL，或由后端代理上传下载；小程序端只接触业务接口，不保存 MinIO 密钥。
- 文件服务首版支持：头像图片、账单凭证图片的上传、预览、替换、删除和失败重试。图片不做 OCR、不自动解析金额、不自动生成账单。
- 文件服务不是独立后台管理页面；设计重点仍放在移动端小程序体验，文件入口只在头像设置和记账详情中出现。
- 设计重点放在移动端小程序体验，不要设计成宽屏后台管理系统。
- 组件和页面应便于后续拆分为可复用的前端组件。

### 2.1 MinIO 文件服务边界

- MinIO 只负责文件二进制存储，不直接承载用户、账单、账户等业务数据。
- Java 后端负责登录鉴权、文件类型与大小校验、对象路径生成、文件元数据、业务归属、访问权限、删除和审计。
- 文件对象必须绑定 `userId`，账单凭证还必须绑定 `bookId` 和 `transactionId`；禁止通过前端传入任意对象路径读取其他用户文件。
- 推荐按业务域划分对象前缀，例如 `avatar/{userId}/...`、`receipt/{userId}/{bookId}/{transactionId}/...`。对象名称使用后端生成的不可预测 ID，不使用原始文件名作为唯一键。
- 推荐至少区分 `private` 桶和 `public` 桶：头像和账单凭证默认放在私有桶，访问时由后端鉴权并签发短时效 URL；不允许把账单凭证放入公共桶。
- 不在数据库中保存永久公开 URL；保存对象 Key、原始文件名、扩展名、MIME 类型、文件大小、宽高（图片）、上传状态、创建时间和删除时间等元数据。
- 上传接口需要支持幂等标识，避免用户重复点击造成重复对象；业务记录保存失败时，已上传但未关联的对象进入待清理状态。
- MinIO 不应作为唯一备份。部署时需要规划数据卷持久化、定期备份、容量监控和故障恢复；设计稿不展示运维控制台，但技术说明必须保留这些要求。

### 2.2 文件服务接口约定（供前后端实现）

接口名称可按 Java 项目规范调整，但职责和返回字段保持一致：

| 接口 | 用途 | 关键入参/返回 |
| --- | --- | --- |
| `POST /api/files/upload-url` | 获取上传授权 | 业务类型、文件名、MIME、大小、幂等 Key；返回 `fileId`、对象 Key、短时效上传 URL |
| `POST /api/files/{fileId}/complete` | 确认上传完成 | `fileId`、文件校验信息；后端校验对象存在后更新状态 |
| `GET /api/files/{fileId}/view-url` | 获取预览地址 | 返回短时效预览 URL；后端校验用户和业务归属 |
| `DELETE /api/files/{fileId}` | 删除文件 | 软删除元数据并异步删除 MinIO 对象；重复删除应幂等 |
| `POST /api/transactions/{id}/attachments` | 关联账单凭证 | `fileId`；只允许关联当前用户可访问的文件 |

文件状态至少包含：`UPLOADING`、`READY`、`FAILED`、`DELETING`、`DELETED`。前端根据状态展示进度、重试、预览或删除，不把已创建但未完成上传的文件当作可用凭证。

### 2.3 文件限制与安全要求

- 首版允许 JPG、JPEG、PNG、WEBP；单张图片建议不超过 10MB，单笔账单最多 9 张凭证，头像最多 1 张。具体数值可由后端配置，但必须在页面上给出清晰限制。
- 前端先做类型、大小和数量校验，后端必须再次校验 MIME、文件头和大小；不能只相信文件扩展名。
- 图片上传前可在客户端压缩到适合预览的尺寸，但原图是否保留由后端配置决定；压缩失败需要允许重试或取消，不得阻塞保存账单。
- 文件名、备注和预览内容按不可信输入处理；下载响应设置正确的 `Content-Type` 和安全的 `Content-Disposition`，避免脚本化文件被浏览器直接执行。
- 头像和凭证均属于用户隐私数据。删除账单时默认同步解除凭证关联并进入可回收/异步删除流程；数据清除功能执行时必须同时删除数据库元数据和 MinIO 对象。
- 网络失败、上传超时、URL 过期、MinIO 不可用、文件格式不支持和空间不足都需要有明确错误文案与重试入口。

### 2.4 MinIO 部署与环境配置建议

- MinIO 服务只允许 Java 文件服务在内网或受控网络访问；小程序不直接访问 MinIO Console，也不暴露管理端口。
- 生产环境必须使用 HTTPS 域名和反向代理。若采用预签名 URL 直传，上传域名需要加入微信小程序合法域名，并配置最小范围的 CORS；若由 Java 后端中转，则小程序只配置 Java API 域名。
- Access Key、Secret Key、桶名、Endpoint、区域、最大文件大小和 URL 有效期全部通过环境变量或密钥配置注入，不写入前端代码、设计稿示例或 Git 仓库。
- 建议创建私有桶，例如 `app-private`，默认禁止匿名读写；Java 服务账号只授予必要的对象读写权限，不使用 MinIO 管理员账号处理业务请求。
- 至少配置 `dev`、`test`、`prod` 三套独立桶或独立 MinIO 环境，避免测试文件污染生产数据。
- 建议为上传未完成、业务关联失败的对象配置定时清理策略；清理前以数据库 `fileId` 和上传状态为准，避免误删已关联文件。
- 需要监控存储容量、上传失败率、响应耗时、签名 URL 失败率和待清理对象数量；MinIO 故障时，账单文字数据仍应能正常保存。
- 若未来需要多节点高可用、跨地域复制或大规模文件，另行评估 MinIO 的分布式部署和备份方案，第一版设计稿不表现这些运维控制台。

## 3. 整体视觉方向

### 3.1 视觉关键词

清新、简洁、柔和、留白、扁平、圆润、轻量、易读、生活化、现代感。

整体观感应接近一款温和、自然、容易长期使用的移动端生活工具 App：现代扁平化为基础，配合圆角卡片、简洁线性图标、低对比度自然插画和轻微阴影。

### 3.2 明确排除的风格

不要使用以下视觉方向：

- 经典拟态风格。
- 玻璃拟态风格。
- 大面积纯黑、深紫、荧光色或强烈撞色。
- 复杂渐变、强烈发光、高光反射。
- 金属、木纹、皮革、实体旋钮等复古材质。
- 写实照片、3D 建模、复杂纹理。
- 过度装饰、过度拥挤或信息密度过高的界面。

### 3.3 哈记账品牌资产使用

- 以目录中的 `哈记账.png` 为唯一品牌图标参考；不得重新绘制成金融机构、钱包或复杂吉祥物风格。
- 图标适合用于首次使用页、原型外层标识、个人页默认头像和关于页品牌信息；业务列表图标仍使用统一线性分类图标。
- 品牌图标保留圆角和青绿色/珊瑚色识别关系；在 24px、32px、56px 三档尺寸下保证轮廓清晰。
- 不把品牌图标当作收入、支出、负债或上传状态的颜色编码；这些状态必须同时提供文字或符号。

## 4. 设计系统

### 4.1 颜色

请使用 CSS 变量或设计令牌统一管理颜色。推荐颜色如下：

| 设计用途 | 颜色变量 | 色值 |
| --- | --- | --- |
| 页面背景 | `--background` | `#F7F8F7` |
| 卡片/表面 | `--surface` | `#FFFFFF` |
| 主色 | `--primary` | `#49AD9C` |
| 主色浅色背景 | `--primary-light` | `#DFF2EF` |
| 主色深色/按压态 | `--primary-dark` | `#278879` |
| 主要文字 | `--text-primary` | `#171A1A` |
| 次要文字 | `--text-secondary` | `#858B8B` |
| 辅助文字 | `--text-tertiary` | `#B0B5B4` |
| 收入金额 | `--income` | `#FF5B5B` |
| 支出金额 | `--expense` | `#278879` |
| 负债金额 | `--liability` | `#E58A5B` |
| 分割线 | `--divider` | `#F0F1F1` |

颜色使用规则：

- 页面背景使用浅灰色或暖白色。
- 卡片使用纯白色。
- 青绿色、薄荷绿或浅绿色作为主要品牌色。
- 重点数字、主要按钮和激活图标使用青绿色。
- 收入金额使用柔和红色 `#FF5B5B`，支出金额使用 `--expense`，负债金额使用 `--liability`；同时必须配合“收入/支出/负债”文字或符号，不能只靠颜色传达含义。
- 普通文字使用深灰色，辅助文字使用中灰色或浅灰色。
- 控制整体颜色数量，避免同时使用过多高饱和颜色。

### 4.2 布局与间距

- 以移动端优先设计。
- 页面左右安全边距：16px～20px。
- 卡片之间间距：12px～16px。
- 大模块之间间距：20px～28px。
- 使用清晰的纵向信息层级。
- 保留充足留白，避免页面拥挤。
- 重要数据和主要操作必须明显突出。
- 所有触控目标建议不小于 44px。

### 4.3 卡片

- 白色背景。
- 圆角 12px～16px。
- 内边距充足，避免内容贴边。
- 只使用非常轻的阴影区分层级。
- 不使用厚重边框。
- 不使用玻璃、毛玻璃、复杂渐变或强烈发光效果。

### 4.4 字体与字号

优先使用系统中文字体：`PingFang SC`、`Microsoft YaHei`、`Noto Sans SC`、`system-ui`。

推荐字号层级：

| 内容层级 | 建议字号 |
| --- | --- |
| 页面标题 | 24px～28px |
| 模块标题 | 18px～22px |
| 普通正文 | 16px |
| 列表标题 | 16px～18px |
| 辅助信息 | 13px～14px |
| 核心金额/重点数字 | 28px～40px |
| 底部导航文字 | 11px～13px |

文字规则：

- 标题使用中等或半粗字重。
- 不要让所有文字都使用粗体。
- 核心金额和关键数字应有明显视觉层级。
- 辅助信息使用灰色。
- 保持舒适、清晰的中文阅读层级。
- 不使用花体、手写体或装饰性字体。

### 4.5 图标

- 使用简洁、圆润、统一风格的线性图标。
- 图标线宽建议 2px～2.5px。
- 图标大小和视觉重量保持一致。
- 当前激活状态使用青绿色。
- 未激活状态使用浅灰色。
- 不要混用填充图标、线性图标和不同来源的图标风格。
- 图标必须语义清晰，并具备足够点击区域。

### 4.6 插画

- 使用扁平化插画。
- 可使用植物、叶片、花朵、山丘、云朵等自然元素。
- 形状圆润、柔和、略带不规则感。
- 颜色低饱和、低对比度。
- 主要放在卡片底部、背景角落或空状态区域。
- 不得影响文字、金额和数据阅读。
- 优先使用 SVG 或 CSS 图形表达。

## 5. 页面与功能结构

第一版底部导航固定包含 4 个入口，使用图标加文字：

1. 首页
2. 日历
3. 资产
4. 我的

统计功能第一版暂不开放，但需要在信息架构和组件设计中预留后续扩展能力；统计不作为第一版底部导航入口。

建议至少输出以下第一版页面设计稿：

- 首页
- 新增/编辑记账页（全屏流程）
- 日历页
- 资产页
- 我的页面
- 账单详情/编辑状态
- 新增资产账户和编辑资产账户状态
- 周期记账设置状态
- 空状态、加载状态、错误状态、删除确认状态

可以额外输出统计页的低保真信息架构或预留说明，但不要把统计功能表现为第一版已上线功能。

## 6. 页面详细需求

### 6.1 首页

首页是用户最常使用的页面，重点是快速查看本月收支和快速记账。

建议结构，从上到下：

1. 顶部导航：首页原型不重复显示“哈记账”和“首页”文字，保留品牌图标、简洁说明文案及右侧搜索操作；其他页面可显示对应页面标题。
2. 数据概览卡片：使用浅青绿色背景，仅重点展示“日均消费”和“本月支出”。“本月支出”应作为核心数字突出显示。
3. 轻量自然插画：放在概览卡片右下角或背景角落，例如叶片、山丘、云朵；插画不能遮挡数据。
4. 每日消费记录：在概览卡片下方按日期展示每天的账单；每个日期分组使用柔和浅白色凸起卡片承载记录。日期分组标题右侧显示当天“收”和“支”的汇总金额，其中“收”为收入红色，“支”为支出绿色；账单图标仅区分支出、收入、转账三类，收入使用小猪存钱罐图标，支出使用人民币硬币图标，转账使用带左右转账提示的信用卡图标；图标需在统一容器内完整居中显示；记录标题使用对应类型文字并直接附着对应底色，三类标签使用统一尺寸并居中文字，不使用具体消费名称或重复的右侧类型标签；每条记录包含图标、标题、时间、资金账户和金额，金额右对齐。
5. 记账入口：页面右下角固定放置透明描边风格的“+”图标，用于新增记账。图标需要有足够点击区域，不能遮挡重要账单内容。
6. 底部导航：白色背景，包含“首页、日历、资产、我的”，当前入口使用青绿色，其余入口使用浅灰色。

首页需要体现“先看懂，再操作”：先展示日均消费和本月支出，再展示每日消费记录，最后通过右下角“+”完成快速记账。每条记账记录都可以点击进入详情并修改。

### 6.2 新增/编辑记账

这是最重要的操作流程，需要尽量减少填写负担。

主方案使用独立全屏页面：金额输入、分类、账户和备注集中在同一流程中，适合键盘输入和编辑回填。分类、账户、日期时间等选择器使用靠近触发控件的弹窗或微信原生选择器。小屏设备不使用高度不足的全屏表单弹层；如需展示弹窗，只展示选择器、删除确认和保存成功反馈等短任务，并优先显示在触发控件附近，空间不足时自动避让。

顶部需要提供记账类型切换，包含：支出、收入、转账。

建议字段与操作：

- 支出/收入/转账切换。
- 金额输入区，金额应成为页面最突出元素。
- 数字计算键盘：支持 `+`、`-`、`×`、`÷` 自动计算，用户可以连续输入简单算式，实时显示计算结果。
- 分类选择，例如餐饮、交通、购物、娱乐、住房、医疗、工资、其他。
- 日期与时间。
- 资金账户（原“支付渠道”）：例如微信、支付宝、银行卡、现金、其他；名称统一使用“账户”，避免用户误以为只是支付方式。
- 备注输入。
- 账单凭证（可选）：支持从相册选择或拍照上传图片，最多 9 张；显示缩略图、上传中进度、上传失败重试、预览和删除。凭证不参与金额识别，不能替代金额、分类和账户字段。
- 保存按钮。

转账类型需要额外展示转出账户和转入账户，不能把转账误计入收入或支出。

交互要求：

- 收入状态使用柔和红色区分，但整体仍保持统一的青绿色品牌视觉。
- 金额输入和计算键盘要清晰、易操作，避免复杂装饰；除法、连续运算、清除和退格等状态需要有明确反馈。
- 分类使用统一风格的圆润线性图标。
- 保存按钮在信息不完整时显示禁用状态，在提交时显示加载状态。
- 保存成功后提供轻量反馈并返回上一页面或刷新账单。
- 已保存的每条记账记录都可以点击进入编辑页面，修改金额、类型、分类、账户、日期、时间和备注。
- 已保存账单的凭证可在详情中预览、追加、替换和删除；凭证上传失败不影响已保存账单，但需要明确提示“凭证未上传成功”。
- 取消、返回或关闭操作需要避免误触丢失内容；有未保存内容时可弹出确认提示。

### 6.3 日历页

用于按日期查看每日收支。

建议结构：

- 顶部显示当前年月，并支持切换年份和月份。
- 日历区域使用简洁、留白充足的设计。
- 有记账的日期可以用小圆点或轻量标记提示，不要使用高饱和色块铺满日期。
- 日历支持选择具体某一天，选中日期使用青绿色突出。
- 日历下方展示所选日期当天的消费记录。
- 无记录日期显示友好的空状态和“添加记账”操作入口。
- 当天的每条记录都可以点击进入详情并修改。

### 6.4 资产页

用于查看和管理用户的资产与账户。

建议结构：

- 顶部资产概览区域依次展示：净资产、总资产、总负债。文案统一使用“总负债”，不使用容易产生歧义的“负资产”。
- 净资产作为核心数据突出展示，三项数据需要有清晰但克制的视觉层级。计算关系明确为：净资产 = 总资产 - 总负债。
- 下方分组展示“信贷账户”和“资金账户”。
- 信贷账户可包含信用卡、花呗、借呗等负债类账户；资金账户可包含微信、支付宝、银行卡、现金等资金类账户。
- 每个账户以白色圆角卡片或列表项呈现，包含账户名称、账户类型、当前余额/欠款和可选的备注。
- 每个账户都支持点击进入编辑页面。账户类型不可直接切换为会改变余额含义的另一类；需要变更时提示新建账户并迁移/核对余额。
- 右下角固定放置透明描边风格的“+”图标，用于新增信贷账户或资金账户。
- 新增账户时需要选择“资金账户”或“信贷账户”，并根据类型展示相应表单字段。首次创建时需要填写期初余额；期初余额不生成一条普通消费记录。
- 没有任何账户时展示空状态，并引导用户通过“+”新增账户。
- 不使用复杂表格线，金额和账户名称需要易于快速阅读。

### 6.5 统计页

统计功能第一版不实现，但需要为后续版本预留页面入口、路由和组件扩展位置。

设计稿中如需展示统计相关内容，仅输出低保真占位或后续规划，不要将统计放入第一版底部导航，也不要让用户误以为该功能已经可用。

后续统计页可以用于帮助用户理解消费结构和变化趋势，预留以下内容：

建议展示：

- 月份切换。
- 本月收入、本月支出、本月结余。
- 收支趋势图，例如按日或按周展示。
- 支出分类占比，例如餐饮、交通、购物等。
- 分类排行榜或消费明细摘要。
- 与上月相比的变化信息。

图表要求：

- 视觉风格简洁、低对比度、易读。
- 图表主色优先使用青绿色，辅助颜色使用低饱和的同色系颜色。
- 不使用复杂 3D 图表、炫目的渐变或过多图例。
- 图表必须有清晰标题、单位和空数据状态。
- 数据较少时也要保持布局稳定，不要出现大面积空白或异常拉伸。

### 6.6 我的页面

用于个人信息、偏好设置和产品辅助功能。

建议结构：

- 顶部展示微信头像；首次使用时需要设计微信授权获取头像的状态，未授权时显示默认头像和授权引导。
- 头像支持从微信授权结果或相册选择图片后上传至文件服务；展示上传中、上传失败重试和更换头像状态。头像上传失败时保留默认头像和继续使用能力。
- 头像下方展示累计记账天数，文案格式建议为“累计记账 X 日”。
- 下方提供“周期记账”功能入口，可用于设置周期性记账规则。
- 周期记账需要支持查看已有规则、新增规则、编辑规则、暂停/启用规则。
- 周期规则可包含金额、收入/支出类型、分类、账户、周期频率、执行日期和备注。
- 账本管理。
- 分类管理。
- 资金账户管理。
- 数据偏好或记账提醒设置。
- 关于与帮助。
- 退出或清除相关操作（如产品需要）。

页面应保持简洁，不要设计成复杂的后台设置页面。使用分组列表和清晰的右侧箭头/开关表达层级。

## 7. 通用组件

请建立可复用的组件体系，至少考虑以下组件：

- 顶部导航栏。
- 底部导航栏。
- 数据概览卡片。
- 金额展示组件。
- 收入/支出切换控件。
- 支出/收入/转账类型切换控件。
- 加减乘除计算键盘。
- 分类图标组件。
- 账单列表项。
- 可点击编辑的记账记录详情。
- 日期分组标题。
- 日历年月切换与日期选择器。
- 资产概览卡片。
- 信贷账户列表项。
- 资金账户列表项。
- 新增/编辑资产账户表单。
- 周期记账规则列表与编辑表单。
- 搜索框。
- 筛选标签或筛选抽屉。
- 主要按钮、次要按钮、文字按钮。
- 悬浮操作按钮。
- 就近定位的选择器和短任务弹窗。
- 确认弹窗。
- Toast 或轻量反馈。
- 空状态、加载状态、错误状态。
- 图表卡片。
- 文件上传器：选择图片、拍照、上传进度、重试、取消、预览、删除和数量上限提示。
- 图片缩略图/预览查看器：支持加载中、加载失败、URL 过期和关闭。
- 账单凭证区域：空状态、已上传、上传中、失败和删除确认。

所有列表尽量使用数据驱动方式渲染，组件应支持默认、悬停（如适用）、点击、禁用、加载、成功和错误等状态。

## 8. 交互与动效

- 按钮点击时可轻微缩放至 `0.97`。
- 卡片点击时改变背景色或增加轻微反馈。
- 页面切换使用淡入或平滑滑动。
- 选择器、确认和短任务弹窗从触发控件附近平滑出现，不统一贴在屏幕底部；空间不足时允许在触发控件上方显示。弹窗遮罩、宽度、最大高度和安全边距必须受当前手机画布限制，不得超出屏幕。
- 动效时长建议 180ms～280ms。
- 动效需要轻柔、快速、克制。
- 禁止大幅弹跳、旋转、闪烁、粒子和强烈发光效果。
- 兼容 `prefers-reduced-motion`，减少或关闭非必要动效。
- 重要操作需要有成功、失败、加载和禁用反馈。
- 删除账单等不可逆操作需要二次确认。

## 9. 响应式与可访问性

- 移动端使用单列布局。
- 内容区域保留 16px～20px 左右边距。
- 底部导航适配移动端安全区域。
- 第一版底部导航仅包含：首页、日历、资产、我的；统计功能只做预留。
- 所有按钮和点击区域至少约 44px。
- 适配 320px 宽度，不能出现文字重叠、按钮溢出或横向滚动。
- 所有页面的整体容器固定在视口内，不允许页面级纵向或横向滑动；仅账单记录、账户记录等记录模块允许在模块内部独立滚动，滚动不得带动页面整体移动。
- 平板和桌面端可以将内容居中。
- 桌面端最大内容宽度建议 480px～720px。
- 不要把移动端页面简单拉伸成宽屏后台管理系统。
- 使用语义化 HTML 或等价的可访问性结构。
- 为按钮、图标、输入框、切换控件添加可理解的无障碍名称和状态。
- 颜色不能成为传达收入/支出的唯一方式，必要时同时使用文字、符号或标签。
- 保证文字与背景有足够对比度。

## 10. 页面状态

请在设计稿中明确展示以下状态：

- 首次使用、没有任何账单。
- 当前月份没有数据。
- 搜索无结果。
- 筛选无结果。
- 数据加载中。
- 网络请求失败。
- 保存记账中。
- 保存成功。
- 计算器输入中、算式错误、除数为零和计算结果确认。
- 转账时的转出账户与转入账户选择。
- 微信头像未授权、授权中和授权成功。
- 头像选择、上传中、上传成功、上传失败和更换头像。
- 账单凭证为空、选择中、上传中、上传成功、上传失败、预览加载中、预览失败、删除确认和数量已达上限。
- 文件类型不支持、文件超过大小限制、网络超时、上传 URL 过期、文件服务不可用。
- 资产账户为空、新增账户、编辑账户和账户保存成功。
- 周期记账为空、已有规则、启用、暂停和编辑规则。
- 删除确认。
- 表单填写不完整或输入错误。
- 底部导航当前选中与未选中状态。
- 按钮默认、点击、禁用和加载状态。

空状态和错误状态可以使用低对比度植物、叶片、云朵等插画，但插画要服务于引导操作，不能喧宾夺主。

## 11. AI 生成设计稿时的输出要求

请基于以上规范输出一套完整、统一的微信小程序 UI 设计稿，第一版至少包含：首页、新增/编辑记账、日历、资产、我的五类核心页面，以及关键弹层和状态页。统计功能第一版不做高保真页面，只需保留后续扩展所需的信息架构和组件位置。

输出时请遵循：

1. 先建立统一的设计系统，再生成各页面，确保颜色、字号、圆角、间距、图标和按钮风格一致。
2. 所有页面使用移动端单列布局，并体现微信小程序的安全区域和第一版底部导航：首页、日历、资产、我的。
3. 优先突出信息层级、金额可读性和记账效率。
4. 每个页面都要说明页面目的、主要模块、用户操作和关键状态。
5. 给出关键组件的默认态、点击态、禁用态、加载态和错误态。
6. 插画只作为辅助氛围元素，不能遮挡或削弱数据阅读。
7. 设计稿应能指导后续前端开发，标注主要尺寸、间距、颜色、字号、圆角和交互行为。
8. 需要提供适用于 320px、375px、414px 宽度的布局说明。
9. 明确展示首页的日均消费、本月支出和每日账单；日历的年月切换、日期选择和当天记录；资产页的净资产、总资产、总负债、信贷账户、资金账户及新增/编辑账户；我的页面的微信头像授权、累计记账天数和周期记账功能。
10. 明确展示右下角“+”图标新增记账或新增资产账户的入口，并确保不遮挡内容。
11. 明确展示支出、收入、转账三种记账类型，以及支持加减乘除的金额计算键盘。
12. 明确说明每条记账记录都可以点击进入详情并修改。
13. 设计结果必须保持清新、柔和、自然、现代、生活化，不要偏向金融后台、复杂数据驾驶舱或复古实体设备风格。

## 12. 当前设计稿需要补齐的问题

原稿的视觉和页面方向清楚，但以下内容如果不补齐，设计稿容易出现页面之间口径不一致，前端也无法准确实现：

| 问题 | 影响 | 本稿补充的处理方式 |
| --- | --- | --- |
| “支付渠道”和“账户”混用 | 账单无法正确影响资产余额 | 统一使用“资金账户”；每笔非转账账单必须关联一个账户 |
| “负资产”概念不清 | 用户不清楚信用卡欠款如何计算 | 改为“总负债”，净资产 = 总资产 - 总负债 |
| 新增记账使用页面还是弹层未定 | AI 可能输出不适合输入的窄弹层 | 记账主流程固定为全屏页面，选择器使用就近弹窗 |
| 月份、日期和日均消费口径缺失 | 不同页面的数字可能对不上 | 统一按用户本地时间和自然月计算，并明确统计口径 |
| 金额计算器只有运算符清单 | 小数、连续运算、错误恢复无法落地 | 增加输入限制、错误提示、确认和回填规则 |
| 只写了“可编辑” | 编辑、删除后资产和列表如何刷新不明确 | 增加保存、删除、撤销/确认和余额重算规则 |
| 首次使用只提到头像授权 | 没有账本、账户、默认分类的首次路径 | 增加轻量初始化流程和授权可跳过策略 |
| 状态清单没有和页面绑定 | 设计师可能遗漏关键状态 | 给每个核心页面规定必须展示的状态和操作 |
| AI 输出要求偏抽象 | 生成结果可能好看但不可开发 | 增加页面清单、标注格式、验收标准和禁止事项 |

## 13. 第一版业务规则与数据口径

### 13.1 用户、账本和本地时间

- 第一版默认一个用户、一个默认账本，账本名称可在“我的 - 账本管理”中修改。
- 微信登录用于识别用户，不强制要求先授权头像；拒绝头像授权仍可正常记账。
- 所有日期展示和月份统计使用用户设备本地时间，账单保存时间同时记录创建时间和用户选择的记账时间。
- 默认打开当前自然月；跨月查看通过月份切换完成。未来日期允许手动补记，但不能进入“日均消费”的已过去天数分母。
- 金额单位固定为人民币元，精确到 0.01 元；所有金额显示两位小数或按统一金额格式省略无意义的末尾 0。

### 13.2 账单类型与账户影响

| 类型 | 必填字段 | 对账户的影响 | 是否进入收支统计 |
| --- | --- | --- | --- |
| 支出 | 金额、分类、账户、日期 | 资金账户余额减少；信贷账户欠款增加 | 计入支出 |
| 收入 | 金额、分类、账户、日期 | 资金账户余额增加；原则上不允许记入信贷账户 | 计入收入 |
| 转账 | 金额、转出账户、转入账户、日期 | 转出账户减少，转入账户增加 | 不计入收入或支出 |

- 转账的转出账户和转入账户必须不同，账户余额不足时第一版只提示，不阻止记账；是否允许透支由账户类型规则决定。
- 信贷账户的余额用“当前欠款”表达，界面显示正数欠款；总负债为所有信贷账户欠款之和，不显示为负数再参与相减。
- 账户余额由期初余额和账单流水计算得出。编辑或删除账单后，相关账户余额、资产概览、首页月度数据和日历数据必须同步刷新。
- 账户删除前必须展示影响说明；有历史账单的账户不可直接物理删除，默认改为“停用”，历史账单仍可查看。无历史账单的账户可删除并二次确认。
- 第一版不支持一笔账单关联多个账户、不支持拆分账单、不支持分期和手续费；设计稿不得生成这些入口。

### 13.3 首页统计口径

- “本月支出”只统计当前账本、当前自然月内的支出，不含转账。
- “本月收入”可在首页概览中作为次要信息展示；原稿要求的核心数字仍是“本月支出”。
- “本月结余” = 本月收入 - 本月支出。
- “日均消费” = 本月已过去日期的支出总额 ÷ 本月已过去天数；当天按设备本地时间计算，未来日期不计入分母。
- 本月尚未产生支出时，日均消费显示“¥0.00”，并配合“记下一笔，开始了解消费”引导，不显示除数错误或空白。
- 首页账单按日期倒序分组，日期组内按记账时间倒序；同一天同时展示收入、支出和转账，但用标签明确类型。

### 13.4 分类与周期记账

- 首次进入时提供不可重复的默认分类：支出包含餐饮、交通、购物、娱乐、住房、医疗、日用、其他；收入包含工资、奖金、兼职、退款、其他。
- 分类至少包含名称、类型、图标、排序和启用状态。停用分类不出现在新增账单选择器中，但历史记录保留原分类。
- 周期记账是“自动生成待确认账单”还是“自动入账”必须在设计中明确。第一版采用更安全的“到期生成待确认记录”，用户确认后才影响余额和统计。
- 周期规则至少包含名称、收支类型、金额、分类、账户、频率、首次执行日、下次执行日、结束方式、备注和启用状态。
- 暂停规则不删除历史已确认账单；恢复后从下一次执行日继续，不补生成暂停期间的记录。

### 13.5 文件与凭证规则

- 文件服务使用 MinIO，但所有文件操作必须经过 Java 后端鉴权；小程序端不得拼接 MinIO 地址、保存永久文件 URL 或接触密钥。
- 账单凭证是账单的可选附件，不改变账单业务状态。账单可以在没有凭证的情况下保存。
- 上传流程为：选择图片 → 前端校验 → 获取上传授权 → 上传 MinIO → 后端确认 → 关联账单。任一步失败都要保留可重试状态，并避免重复生成账单。
- 保存账单时若凭证正在上传，主按钮应显示“保存并继续上传”或阻止提交，二者必须选定一种全局一致的行为。推荐允许先保存账单，凭证在详情页继续上传，并明确展示附件未完成。
- 替换凭证先上传新文件并确认成功，再删除旧文件，避免网络失败导致凭证丢失。
- 预览使用短时效 URL；URL 过期时自动重新获取一次，仍失败则显示“暂时无法查看，请重试”。
- 删除凭证必须二次确认，确认后从账单详情立即移除；后端异步删除对象，删除失败需要进入可重试的清理状态。
- 账单删除、账本清除和用户注销等操作必须同步考虑凭证元数据与 MinIO 对象，不能只删除数据库记录造成孤儿文件。

## 14. 核心流程和页面状态补全

### 14.1 首次使用流程

设计稿至少展示以下 3 个状态：

1. 首次打开：说明“记录每一笔，让生活更清晰”，提供“开始记账”；头像授权为次要操作，不阻塞主流程。
2. 初始化账户：建议快速创建“微信”资金账户，期初余额可填可跳过；同时展示“稍后设置”。
3. 初始化完成：进入首页空状态，突出右下角“+”和“添加第一笔记账”。

首次使用不应强制弹出多个授权框、提醒权限或账户表单。微信头像授权需要有未授权、授权中、授权成功和拒绝后的可继续使用状态。

### 14.2 新增/编辑记账页面

- 顶部返回按钮、页面标题（新增记账/编辑记账）和右上角删除入口（仅编辑态）。
- 第一行是三段式类型切换：支出、收入、转账；切换类型时保留金额和日期，但清空不适用字段并给出轻提示。
- 金额区支持最多 2 位小数、单次金额最大值 999999999.99；输入为空显示占位符，不显示伪造的 0。
- 计算器支持 `+ - × ÷`、小数点、清除、退格和等号。按标准运算优先级计算；连续运算时展示完整算式和当前结果；除数为 0、连续运算符、超过金额上限时在金额区内联报错，并允许退格修正。
- 用户点击保存前，最终显示的是计算结果而不是算式；保存后再次编辑时回填结果，不回填原始算式。
- 支出默认选中最近使用分类和账户（若存在），收入默认选中最近使用的收入分类；首次使用则显示“请选择”。
- 未选择必填字段时保存按钮禁用；输入错误时按钮可点击但提交前聚焦错误字段并说明原因，避免用户不知道为什么不能保存。
- 编辑态支持保存、取消和删除。删除按钮采用文字按钮或更多菜单，避免与保存按钮竞争视觉焦点。
- 有未保存变更时返回、关闭或切换页面，弹窗提供“继续编辑”和“放弃更改”；没有变更时直接返回。

### 14.3 日历页

- 月份切换可用左右箭头和年月选择器；切换月份后默认选中当月 1 日，有记录时可默认选中最近有记录的日期，但行为必须全局一致。
- 每个日期最多显示一个低对比度记录点；收入和支出同时存在时使用双点或小标签，不使用大色块。
- 日期下方提供所选日小结：收入、支出、当日结余；转账单独显示笔数或金额，但不混入收支结余。
- 记录列表支持点击编辑、左滑删除（如设计工具无法表达左滑，则使用更多菜单替代），删除后日历标记和小结立即刷新。
- 当前日期、选中日期、有记录日期和不可选日期需要视觉区分，不能只靠颜色。

### 14.4 资产页

- 概览卡片明确标注“净资产 / 总资产 / 总负债”，并提供计算关系的轻量说明或可访问性标签。
- 资金账户按余额从高到低或用户自定义顺序展示；信贷账户按欠款从高到低展示。排序规则在设计稿中注明。
- 新增账户表单字段：账户名称、账户归属（资金/信贷）、账户类型、期初余额、是否计入净资产、备注。账户类型切换时同步更新字段文案，如“期初余额/期初欠款”。
- 编辑账户只修改账户资料；余额调整必须通过“余额校准”入口，生成一条可追溯的校准记录，避免直接改余额造成流水对不上。若第一版不做余额校准，必须将余额字段设为只读并明确提示“请通过记账或转账调整”。
- 资产为空时显示“先添加一个常用账户”；有账户但无流水时显示账户余额和“记下一笔”；请求失败时保留已加载数据并提供重试。

### 14.5 我的页面与设置

- “资金账户管理”与资产页是同一数据入口，避免维护两套账户概念；我的页面可保留入口，但需标注跳转到资产页的账户管理区域。
- “记账提醒”若第一版只做占位，应使用“即将支持”或隐藏，不生成不可用的开关；若实现，则明确提醒时间、重复规则、关闭和系统权限拒绝状态。
- “退出登录”与“清除本地数据”不是同一操作。清除数据必须单独放在危险区域，明确不可恢复范围，并要求二次确认；第一版若无此能力则不要放入口。
- 关于与帮助至少包含版本号、隐私说明、数据导出/删除能力的状态说明。没有后端能力时不得设计“已支持导出”的按钮。

## 15. 组件状态、反馈和可访问性补充

### 15.1 统一状态规范

| 状态 | 设计要求 |
| --- | --- |
| 默认 | 显示正常内容、可操作控件和清晰层级 |
| 按压 | 颜色或背景轻微变化，按钮缩放至 0.97，180ms～280ms |
| 禁用 | 降低对比度但仍满足文字可读性，并解释禁用原因 |
| 加载 | 保留页面结构，用骨架或按钮内加载，不用整页无意义转圈 |
| 空 | 说明当前没有数据，提供一个与上下文直接相关的主操作 |
| 错误 | 说明发生了什么、是否已保存、下一步怎么做，并提供重试 |
| 成功 | Toast 或轻提示说明结果，必要时提供“查看”操作；不依赖颜色单独表达 |
| 删除 | 明确对象、影响和不可逆性；有历史数据时优先提供停用而不是删除 |

### 15.2 输入和金额可读性

- 金额统一显示 `¥` 和金额，列表金额右对齐；收入前可加“+”，支出前可加“−”，转账使用独立“转账”标签。
- 键盘弹出时保存按钮和错误提示不能被遮挡；点击金额区、分类区、备注区时键盘状态应可预期。
- 文本输入需要限制最大长度：账户名 20 字、分类名 12 字、备注 100 字，并在接近上限时提供计数或轻提示。
- 所有图标按钮必须有文字标签、无障碍名称或可理解的提示；颜色对比度符合 WCAG AA 的基本要求。

## 16. 设计稿交付格式与验收标准

### 16.1 必须交付的画板/页面

请按以下顺序输出高保真页面或画板，并在每个画板中标注页面名称和状态：

1. 设计令牌与组件总览：颜色、字号、间距、圆角、阴影、图标、按钮和导航。
2. 首次使用与头像授权：未授权、授权中、授权成功、拒绝后可继续使用。
3. 首页：有数据、当前月无数据、无账户、加载、网络错误、搜索无结果、筛选无结果。
4. 新增记账：支出、收入、转账、计算器输入中、计算错误、保存中、保存成功。
5. 编辑记账：回填、未保存退出确认、删除确认、删除成功/失败；账单凭证为空、上传中、上传失败、预览和删除。
6. 日历：有记录、无记录、月份切换、当天记录、加载和错误。
7. 资产：有账户、无账户、账户编辑、新增资金账户、新增信贷账户、保存中和错误。
8. 我的：头像已授权/未授权、头像上传中/失败/更换、周期记账空状态、已有规则、编辑、暂停、启用。
9. 通用反馈：就近选择器、确认弹窗、Toast、空状态、骨架加载、网络错误。
10. 统计预留：只提供低保真路由/组件占位，并标注“第一版暂不开放”。

### 16.2 每个页面必须附带的标注

- 页面目的、进入方式、主要用户任务和离开方式。
- 默认数据、空数据和错误数据的示例；金额示例应覆盖 0、整数、小数、大金额和负债。
- 主要尺寸：页面左右边距、模块间距、卡片内边距、最小触控区域、底部安全区。
- 交互说明：点击、按压、禁用、加载、保存成功、失败、返回和删除。
- 字段必填性、校验规则、默认值、错误文案和数据刷新范围。
- 320px、375px、414px 三种宽度下的布局结果；不允许横向滚动，金额不得被截断，长名称需省略或换行。
- 验收时需确认各页面最右侧无页面滚动条；账单列表、账户列表等记录模块内容超出高度时可独立滑动，页面背景、标题、悬浮按钮和底部导航保持固定。
- 验收时需确认所有弹窗都限制在手机画布内，弹窗宽度不超过屏幕可用宽度，最大高度不超过屏幕安全区域，且不会被手机画布裁切。

### 16.3 AI 生成结果验收清单

- 是否始终只有 4 个第一版底部导航入口：首页、日历、资产、我的。
- 是否在首页同时清晰展示日均消费、本月支出和按日期分组的账单。
- 是否存在支出、收入、转账三种类型，且转账明确有转出账户和转入账户。
- 是否支持加、减、乘、除、清除、退格、等号、除数为零和算式错误状态。
- 是否所有账单都能进入详情编辑，编辑和删除后能说明数据如何刷新。
- 是否资产页明确展示净资产、总资产、总负债，以及信贷账户和资金账户。
- 是否我的页面包含微信头像授权状态、累计记账天数和周期记账入口。
- 是否账单详情支持可选凭证图片的选择、上传进度、失败重试、预览和删除，且没有把凭证上传误设计成 OCR 或自动记账。
- 是否明确使用 MinIO 作为对象存储、Java 后端负责鉴权和元数据，小程序端不暴露 MinIO 密钥和永久地址。
- 是否覆盖文件类型/大小/数量限制、上传 URL 过期、网络失败、MinIO 不可用和孤儿文件清理等状态。
- 是否所有右下角透明描边“+”入口在安全区内且不遮挡最后一条记录；底部导航和悬浮按钮不会互相遮挡。
- 是否没有把统计、预算、自动同步、OCR、多人账本等未上线能力伪装成可用功能。
- 是否所有空、加载、错误、禁用、保存中和删除确认状态都能指导用户下一步操作。

## 17. 给 AI 设计工具的最终执行指令

请以本需求为唯一产品约束，先建立设计令牌和可复用组件，再输出第 16 节规定的移动端微信小程序画板。主内容宽度以 375px 为基准，同时验证 320px 和 414px；页面左右边距 16px，底部预留微信安全区。使用清新、柔和、自然、现代的青绿色设计系统，保留足够留白和高对比度金额层级。

请优先生成真实可操作的页面结构，不要只生成宣传海报或概念图。每个核心页面都要同时提供默认态、空态、加载态、错误态和关键操作态；每笔账单都能点击进入编辑；每个账户都能新增和编辑；所有危险操作都要有确认。新增/编辑记账使用全屏页面，分类、账户和日期选择使用靠近触发控件的弹窗。账单详情提供可选凭证图片入口，展示选择、上传、重试、预览和删除状态，但不要生成 OCR、自动识别金额或自动入账功能。严格遵守第一版仅有“首页、日历、资产、我的”四个底部导航，统计只做低保真预留。

请在设计稿旁提供一份页面-组件-状态映射说明，并以开发可执行的方式标注颜色变量、字号、间距、圆角、触控区域、字段校验、错误文案和页面跳转。不要生成宽屏后台、复杂图表、强渐变、玻璃拟态、写实图片、不可用的设置开关或未在本需求中定义的业务能力。
