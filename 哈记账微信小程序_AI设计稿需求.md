# 哈记账微信小程序｜AI 设计稿需求（原型对齐版）

> 版本：v1.0 · 设计交付基线
>
> 本文以仓库内 `哈记账小程序_原型设计稿/` 的页面、交互和视觉实现为准，是 AI 设计工具、产品、交互和前端的共同约束。若本文与旧版描述冲突，以本版和原型实际表现为准。

## 0. 先读：本次对齐结论

### 0.1 首版已开放

- 底部导航固定为：首页、日历、资产、我的。
- 首页展示本月概览、按日期分组的账单和新增记账入口。
- 新增/编辑记账为独立全屏流程，支持支出、收入、转账。
- 金额键盘支持 `+`、`−`、`×`、`÷`、小数点、退格、清除和等号。
- 日历支持月份切换、年月选择、日期选择、当天收支小结和账单详情。
- 资产支持资金账户、信贷账户、账户详情、流水筛选、新增/编辑/删除。
- 信贷账户支持从资金账户发起还款，并生成还款流水。
- 账单详情支持编辑、删除、收入/支出退款及退款记录管理。
- 我的页面支持微信头像授权状态展示、累计记账天数、关于与帮助、隐私说明。
- 首次使用页支持“开始记账”和“稍后设置账户”。

### 0.2 首版只做占位或说明，不得画成可用功能

- 统计页：只保留路由/信息架构占位，不进入底部导航。
- 数据导出/删除：在关于与帮助页展示“暂未开放”说明，不生成可点击的可用按钮。
- 反馈建议：可保留为“反馈入口已预留”的轻提示，不生成真实提交表单。
- 头像相册选择、头像文件上传：原型只展示微信头像授权中/成功状态；不要扩展成完整图片上传流程。

### 0.3 本版明确不生成

- 账单分类选择器和分类管理入口。原型中的“餐饮、交通、工资”等仅作为示例数据标签或记录来源信息，不是当前记账表单字段。
- 账单凭证、拍照、相册选图、OCR、金额识别和自动入账。
- 周期记账、预算、多人共享账本、支付平台同步、投资收益、多币种、拆分账单、分期和手续费。
- 宽屏后台、复杂统计驾驶舱、强渐变、玻璃拟态、写实图片和金融机构式视觉。

## 1. 产品定位与设计目标

产品名称：哈记账。

核心承诺：“3 秒记下一笔，10 秒看懂当月”。

目标用户：希望轻松记录日常收支、查看账户余额并培养记账习惯的普通用户。

设计目标：

1. 首屏先让用户看懂本月支出和最近账单，再引导记账。
2. 通过金额键盘减少输入步骤，支出、收入、转账的语义必须清楚。
3. 账户余额、总资产、总负债和净资产的口径保持一致。
4. 所有危险操作有明确影响说明和二次确认。
5. 页面既要适合 375px 手机画布，也要能在 320px 和 414px 宽度下稳定呈现。

## 2. 原型页面与路由

| 页面/路由 | 进入方式 | 首版定位 | 关键操作 |
| --- | --- | --- | --- |
| 首次使用 | 首次打开或设计说明中预览 | 高保真 | 开始记账、稍后设置账户 |
| 首页 | 底部导航 | 高保真 | 查看概览、查看账单、新增记账 |
| 新增记账 | 首页/日历/资产流水的 `+` 或相关入口 | 高保真 | 支出、收入、转账、计算、保存 |
| 账单详情 | 点击任意账单 | 高保真 | 编辑、删除、退款、查看退款记录 |
| 日历 | 底部导航 | 高保真 | 月份切换、选日、查看当天账单 |
| 资产 | 底部导航 | 高保真 | 查看净资产、管理账户、查看流水 |
| 资金账户详情 | 资产页点击资金账户 | 高保真 | 流水筛选、编辑、删除 |
| 信贷账户详情 | 资产页点击信贷账户 | 高保真 | 流水筛选、编辑、还款、删除 |
| 编辑还款 | 信贷账户流水中点击还款 | 高保真 | 修改还款账户和金额 |
| 我的 | 底部导航 | 高保真 | 头像授权、关于与帮助 |
| 关于与帮助 | 我的页 | 高保真 | FAQ、隐私说明、预留入口提示 |
| 统计预留 | 不展示为可用入口 | 低保真占位 | 只说明后续扩展方向 |

首版底部导航只能出现四项：首页、日历、资产、我的。新增记账、详情和账户详情属于二级页面，不得加入底部导航。

## 3. 技术与数据边界

- 产品形态：微信小程序；建议 Taro + React + TypeScript，或等价方案。
- 后端：Java，负责登录鉴权、账单、账户、退款、还款和数据口径计算。
- 第一版默认单用户、单账本、人民币单币种。
- 日期和时间以用户设备本地时间展示和统计；保存时同时记录业务记账时间和服务端创建时间。
- 原型阶段使用真实可读的示例数据；正式实现不得把示例数据当作真实用户数据。
- 原型中的头像授权为状态演示。若正式接入头像文件服务，必须由 Java 后端管理文件元数据和权限，小程序不能持有 MinIO 密钥或永久对象地址。
- 账单保存、编辑、删除、退款、还款后，首页、日历、账户流水和资产汇总必须按同一份服务端数据刷新。

### 3.1 未来文件服务约束（本版不生成凭证 UI）

如后续增加头像文件上传或账单凭证，沿用以下边界：

- MinIO 只存储二进制对象，Java 后端负责鉴权、对象 Key、元数据、归属和删除。
- 小程序只调用业务接口和短时效上传/预览 URL，不拼接内部 MinIO 地址，不保存 Access Key/Secret Key。
- 文件对象必须绑定 `userId`；账单附件还要绑定 `bookId` 和 `transactionId`。
- 推荐对象前缀为 `avatar/{userId}/...`、`receipt/{userId}/{bookId}/{transactionId}/...`，对象名使用后端生成的不可预测 ID。
- 文件类型、文件头、大小和权限由后端再次校验；私有对象不得放入公共桶。

## 4. 视觉设计系统

### 4.1 品牌与颜色

使用目录中的 `哈记账.png` 作为唯一品牌标识参考。品牌图标只承担品牌识别，不承担收入、支出、负债或上传状态表达。

| 用途 | 令牌 | 色值 |
| --- | --- | --- |
| 页面背景 | `--background` | `#F7F8F7` |
| 卡片表面 | `--surface` | `#FFFFFF` |
| 品牌主色 | `--primary` | `#49AD9C` |
| 主色深色/按压 | `--primary-dark` | `#278879` |
| 主色浅底 | `--primary-light` | `#DFF2EF` |
| 主要文字 | `--text-primary` | `#171A1A` |
| 次要文字 | `--text-secondary` | `#858B8B` |
| 辅助文字 | `--text-tertiary` | `#B0B5B4` |
| 收入 | `--income` | `#FF5B5B` |
| 支出 | `--expense` | `#278879` |
| 负债/危险操作 | `--liability` | `#E58A5B` / `#D47769` |
| 分割线 | `--divider` | `#F0F1F1` |

颜色不能成为唯一语义。收入、支出、转账、还款、删除等状态必须同时使用文字、图标或符号。

### 4.2 尺寸与布局

- 基准画布：375 × 812px。
- 适配宽度：320px、375px、414px；禁止横向滚动。
- 页面左右内边距：16px；内容最大宽度建议 480px～720px，桌面端居中显示手机画布。
- 卡片圆角：14px～20px；列表项圆角 12px～16px。
- 卡片间距：12px～16px；大模块间距：20px～28px。
- 所有按钮和图标的有效点击区域不小于 44px。
- 底部导航和悬浮 `+` 避开 `env(safe-area-inset-bottom)`，不遮挡最后一条记录。
- 页面整体固定在手机画布内；账单列表、账户流水和详情内容在所属容器内独立滚动。

### 4.3 字体、图标和插画

- 字体优先使用 `PingFang SC`、`Microsoft YaHei`、`Noto Sans SC`、`system-ui`。
- 页面标题 17px～24px，模块标题 16px～18px，正文 13px～16px，辅助信息 10px～12px，核心金额 28px～42px。
- 图标使用圆润、轻量、统一线性风格；账单类型使用原型中的硬币、小猪存钱罐、转账卡片和还款卡片图标。
- 插画仅用于首次使用、摘要卡片、空状态和背景角落，使用低饱和自然元素，不遮挡金额和按钮。
- 禁止拟态、玻璃拟态、大面积黑紫、荧光色、复杂渐变、金属材质和写实照片。

## 5. 页面详细设计要求

### 5.1 首次使用页

目的：让新用户理解产品并直接开始记账，不被授权和账户初始化阻塞。

页面结构：品牌标识、`A LITTLE BOOKKEEPER` 文案、自然叶片插画、核心文案“记录每一笔，让生活更清晰”、辅助文案“用 3 秒记下一笔，用 10 秒看懂这个月”、主按钮“开始记账”、次按钮“稍后设置账户”。

交互：点击“开始记账”进入首页并提示欢迎；点击“稍后设置账户”直接进入首页。不得强制弹出头像授权、通知授权或账户表单。

### 5.2 首页

页面顺序：

1. 顶部显示品牌图标和“记录每一笔，让生活更清晰”，不重复显示“首页”大标题。
2. 浅青绿色概览卡展示当前年月、日均消费、本月支出和本月收入；日均消费与本月支出优先突出。
3. “最近记账”列表按日期倒序分组；日期组右侧展示当天“收”和“支”。
4. 每条记录展示类型图标、类型标签、时间、账户或转出/转入账户、金额和进入详情的箭头。
5. 页面右下角固定透明描边 `+`，用于新增记账。
6. 底部固定四项导航。

首页不展示分类选择器，不展示凭证入口，不展示统计图表。退款后的支出/收入以“退”标识，金额汇总按有效金额刷新。

### 5.3 新增/编辑记账页

必须使用全屏页面，不能把完整记账表单塞进窄弹层。

顶部：返回按钮、标题“新增记账”或“编辑记账”。编辑态返回时，如有未保存内容，显示“继续编辑/放弃更改”确认弹窗。

类型切换：支出、收入、转账三段式控件。收入使用柔和红色强调；转账使用中性色。切换类型时清理不适用的账户字段，并保留金额和日期。

金额区：金额是页面视觉重点；空状态显示“输入金额”，不要伪造 `0`。键盘支持数字、小数点、`+`、`−`、`×`、`÷`、退格、清除和等号；按标准运算优先级计算。

字段：

- 支出/收入：资金账户、日期与时间、备注。
- 转账：转出账户、转入账户、日期与时间、备注。
- 原型不生成分类字段和账单凭证字段。

校验：金额必须大于 0、最多两位小数、最大 `999999999.99`；转出账户和转入账户不能相同；账户选择器只展示可用资金账户；保存中按钮显示加载态并防止重复提交。

保存成功后回到来源页或刷新详情，显示“记账成功，账户余额已更新”。

### 5.4 账单详情、编辑、退款和删除

详情页展示：类型图标、类型标签、金额、完成/部分退款/已退款状态、账户信息、记账时间、备注、账单类型和记录编号。

操作：

- 编辑：回填金额、类型、账户、日期、时间和备注。
- 删除：二次确认，说明会同步刷新首页、日历和相关账户余额；确认后软删除或停用记录，历史影响按后端规则处理。
- 退款：仅支出和收入支持；转账和还款不显示退款按钮。允许部分退款，多次退款累计不超过原账单金额。
- 退款记录：显示累计退款、每笔退款时间和金额；删除退款记录必须二次确认，删除后恢复相应有效金额。

退款只改变有效收支金额和账户余额，不把退款设计成新的“收入/支出录入页”。

### 5.5 日历页

顶部保留品牌图标和“按日期回看每一笔”。日历卡片提供当前年月、上月按钮、“今天”按钮和年月选择器。

有记录日期使用低对比度小圆点标记；不同类型可用不同色点，但必须同时提供底部图例。选中日期使用青绿色突出，今天使用独立视觉标记。

日历下方显示所选日的支出、收入、结余三项小结，以及当天账单列表。转账和还款可显示在列表与图例中，但不计入收入、支出和结余。无记录时显示“这一天还没有记账”。点击记录进入详情。

### 5.6 资产页

顶部资产卡依次展示净资产、总资产、总负债，并明确“净资产 = 总资产 − 总负债”。

下方为可折叠分组：信贷账户、资金账户。账户列表展示图标、名称、余额/欠款、信贷账户可用额度和进入详情箭头。右下角 `+` 打开新增资产账户选择。

新增/编辑资金账户：账户名称、余额、是否计入净资产。编辑余额后，如果余额发生变化，必须询问是否生成“账户余额补齐”流水；选择“加入流水”生成收入或支出校准记录，选择“不加入流水”只更新账户余额，并在产品规则中保留可追溯提示。

新增/编辑信贷账户：账户名称、总额度、当前欠款、是否计入净资产。欠款不能大于总额度，账户名称不能与其他账户重复。

删除账户必须二次确认并说明历史账单保留；正式实现优先使用停用而非物理删除。

### 5.7 账户详情与信贷还款

资金账户详情展示当前余额、是否计入净资产、账户流水和类型筛选：全部、支出、收入、转账、还款。操作为编辑账户、删除账户。

信贷账户详情展示当前欠款、总额度、可用额度、账户流水和同样的类型筛选。操作为编辑账户、还款、删除账户。

还款流程：选择资金账户、输入还款金额、校验金额不超过当前欠款且资金账户余额充足；成功后扣减资金账户余额、扣减信贷账户欠款、恢复可用额度，并新增一笔还款流水。还款流水可再次编辑或删除，编辑/删除需先撤回旧影响再应用新影响。

### 5.8 我的与关于帮助

我的页只展示：默认/授权头像、账本主人、头像授权状态、累计记账 28 日示例和“关于与帮助”入口。

头像状态至少有：未授权、授权中、授权成功。拒绝授权不影响记账；授权中按钮禁用，成功后显示“更换头像”。原型中头像图片仍使用品牌图标作为默认展示，不生成相册上传流程。

关于与帮助页包括：品牌说明、三步快速开始、FAQ、隐私说明、数据导出/删除的“暂未开放”状态、反馈建议预留入口和版本号。FAQ至少解释如何记账、转账为何不算收支、账户余额如何计算、账户是否自动选择。

## 6. 交互、动效和状态规范

- 弹窗采用手机画布内居中短任务弹窗，适用于账户选择、年月选择、备注、还款、退款、确认和隐私说明；不得超出手机画布。
- 按压状态轻微缩放至 `0.97`，动效 180ms～280ms；支持 `prefers-reduced-motion`。
- 加载态保留页面结构，优先在按钮内展示加载，不使用无意义整页转圈。
- 空状态要解释当前没有什么，并提供上下文相关的下一步操作。
- 错误态说明发生了什么、数据是否已保存、如何重试。
- 删除、放弃编辑、删除退款、删除账户等不可逆或高风险操作必须二次确认。
- 输入框必须限制长度：账户名 20 字，备注 100 字；金额按人民币分精度处理。
- 所有图标按钮提供可理解的无障碍名称；收入、支出、负债不能只用颜色区分。

必须覆盖的状态：首次使用、首页有数据/空数据/加载/网络错误、记账空金额/计算中/算式错误/除数为零/保存中/成功、详情部分退款/全部退款/删除确认、日历有记录/无记录/月份切换/加载/错误、资产有账户/无账户/分组收起/账户保存/删除确认、头像未授权/授权中/授权成功、FAQ展开/收起、暂未开放提示。

## 7. 业务规则与数据口径

### 7.1 账单类型

| 类型 | 必填 | 账户影响 | 收支统计 |
| --- | --- | --- | --- |
| 支出 | 金额、资金账户、日期 | 资金账户余额减少 | 计入支出 |
| 收入 | 金额、资金账户、日期 | 资金账户余额增加 | 计入收入 |
| 转账 | 金额、转出账户、转入账户、日期 | 转出减少、转入增加 | 不计入收入/支出 |
| 还款 | 系统从信贷详情发起 | 资金账户减少、信贷欠款减少 | 不计入收入/支出 |

信贷账户使用“当前欠款”表达，界面显示正数；总负债为已启用且计入净资产的信贷账户欠款之和。

### 7.2 金额与汇总

- 单位为人民币元，精确到 0.01 元；服务端建议使用整数分或 Decimal，禁止使用浮点数直接做账务持久化。
- 净资产 = 计入净资产的资金账户余额之和 − 计入净资产的信贷账户欠款之和。
- 本月支出只统计自然月内有效的支出，不含转账、还款和已全额退款部分。
- 本月收入只统计自然月内有效的收入。
- 本月结余 = 本月收入 − 本月支出。
- 日均消费 = 本月已过去日期的有效支出总额 ÷ 本月已过去天数；没有支出时显示 `¥0.00`。
- 退款后首页日期汇总、月度汇总、日历小结和账户余额必须同步刷新。

### 7.3 删除与账户命名

- 账单删除必须可追溯，正式实现推荐软删除。
- 有流水的账户优先停用；停用后历史账单可查看，但新账单不能选择。
- 账户名称在同一账本内唯一，编辑名称需同步更新展示关联，不应破坏历史流水关联。

## 8. AI 设计稿交付清单

请按以下顺序输出画板或页面：

1. 设计令牌与组件总览。
2. 首次使用和头像授权三态。
3. 首页：有数据、空数据、加载、网络错误。
4. 新增记账：支出、收入、转账、计算器输入/错误、保存中/成功。
5. 账单详情：默认、编辑回填、部分退款、全额退款、删除确认。
6. 日历：有记录、无记录、月份选择、日期选择、当天账单。
7. 资产：有账户、无账户、分组收起、新增/编辑资金账户、新增/编辑信贷账户。
8. 账户详情：资金账户流水、信贷账户流水、筛选、还款、编辑还款、删除确认。
9. 我的与关于帮助：头像未授权/授权中/授权成功、FAQ展开、隐私说明、暂未开放状态。
10. 统计低保真占位，并明确“第一版暂不开放”。

每个页面必须标注：进入和离开方式、默认/空/错误数据、左右边距、模块间距、卡片尺寸、触控区域、安全区、字段必填性、校验、错误文案、加载和刷新范围，并验证 320px、375px、414px 三种宽度。

## 9. 最终验收标准

- 首版始终只有：首页、日历、资产、我的四个底部入口。
- 首页同时展示日均消费、本月支出和日期分组账单。
- 记账只出现支出、收入、转账三种手动类型；还款只能从信贷账户流程发起。
- 转账有转出账户和转入账户，且不可相同。
- 金额键盘支持加减乘除、清除、退格、等号、两位小数、除数为零和算式错误。
- 每条账单可进入详情、编辑和删除；支出/收入可退款，退款有累计与明细。
- 资产页明确显示净资产、总资产、总负债，并区分资金账户和信贷账户。
- 信贷账户可发起还款，资金不足、金额超欠款等错误有明确反馈。
- 我的页包含头像授权三态和累计记账天数，不出现周期记账的可用入口。
- 统计、分类、凭证、OCR、自动同步、预算、多人账本等未开放能力没有被设计成可用功能。
- 320px 下无横向滚动；悬浮按钮、底部导航和弹窗均不遮挡内容。
