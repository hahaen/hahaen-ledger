# 2026 年迭代档案

本目录按月份继续划分 2026 年的变更记录。进入某个月份后先阅读该月索引，再进入具体 feature-key；同一月份的多个迭代仍要独立记录需求、实现、验证和回滚。

## 目录约定

月份目录只负责时间索引，不承载跨迭代的技术规范。某一项改动应落在月份下独立的 feature-key 目录中，并沿用 `10-iterations/README.md` 规定的 01–10 文件顺序。

## 归档注意

后续迭代可以引用本年度已有结论，但不能把新的验证结果覆盖到旧档案中；如果旧迭代的状态发生变化，应在新迭代中记录变化原因、关联文件和回归证据。
