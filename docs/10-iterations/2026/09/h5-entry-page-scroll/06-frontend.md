# Frontend

在 `app/src/prototype.scss` 的 H5 条件编译区，明确 `.entry-page` 使用动态视口高度和隐藏页面外溢，`.entry-content` 使用 `flex:1 1 0`、`height:0`、`min-height:0`、`overflow-y:auto` 和 `-webkit-overflow-scrolling:touch`；微信小程序条件区和记账页面模板未修改。
