# `logout-from-help`｜帮助页注销入口

## 迭代目的

本迭代用于明确“我的/帮助”页面中的注销行为，保证用户能从非首页安全结束当前会话，并在注销成功后回到合适的认证入口。

## 应记录的重点

应说明注销 API、按钮 loading/禁用态、成功后的本地会话清理、失败重试、重复点击以及注销后访问受保护页面的行为。注销只能结束会话，不能误删账本、文件或业务数据。

## 当前档案状态

当前工作区未保留本迭代明细文件；本 README 只定义审查范围，不代表注销接口和浏览器流程已验证。
