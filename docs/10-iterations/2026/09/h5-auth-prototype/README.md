# `h5-auth-prototype`｜H5 认证原型

## 迭代目的

本迭代用于验证 H5 登录/注册页面、验证码交互、认证路由和基础 API 调用的可行性，为正式认证实现提供页面与协议反馈。

## 应记录的重点

原型可以验证布局、路由和交互，但不能把 Mock、静态页面或浏览器可打开误认为真实认证完成。正式实现仍需验证密码哈希、会话、失败信息不泄漏、登录审计和权限。

## 当前档案状态

当前工作区未保留本迭代明细文件；本 README 仅说明原型的定位和验收边界。
