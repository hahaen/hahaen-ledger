# `h5-root-login`｜H5 根路由登录入口

## 迭代目的

本迭代用于明确 H5 根路径、登录页、注册页和已登录首页之间的启动跳转规则，避免未登录用户被错误送入业务页，或直接访问认证路由时被无条件覆盖。

## 应记录的重点

应覆盖首次访问、已有会话、会话失效、直接访问登录/注册路由、刷新页面和未知路由等场景，并说明跳转判断应基于认证状态和当前路由，而非简单的全局强制重定向。

## 当前档案状态

当前工作区未保留本迭代明细文件；没有浏览器路径回归证据时，不宣称根路由登录行为已验证。
