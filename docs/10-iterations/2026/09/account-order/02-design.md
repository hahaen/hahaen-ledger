# 设计

在 `asset_account` 增加 `sort_order`，以用户和账户类型为排序域，数字越小越靠前。旧数据由 Flyway 按原有名称、ID顺序回填；新建时由 Service 查询同类最大顺序并加一。

前端资产页继续使用现有账户详情点击行为。长按账户设置待交换账户，页面显示提示条；点击同类账户调用换序接口并刷新资产概览。选中状态下点击原账户可取消，异常时退出换序状态并沿用统一错误提示。

换序请求携带双方当时的顺序值。Service 在事务内按 ID 顺序锁定两个当前用户账户，只有顺序仍匹配时才交换；重复请求发现已经是目标状态时直接幂等返回。
