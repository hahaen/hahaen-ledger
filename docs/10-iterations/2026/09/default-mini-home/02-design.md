# 设计

- 将 `pages/index/index` 放在 `pages.json` 首位，作为 uni-app 默认首路由。
- 移除 `App.vue` 小程序启动认证成功后的首次使用页跳转。
- 移除小程序登录重试成功后的首次使用页跳转。
- 启动完成后异步检查当前页面栈，若仍停留在认证页或首次使用页，则仅在已有有效 Token 时重载首页。
- 保留小程序静默登录、会话失效重登和首页业务数据加载逻辑。
