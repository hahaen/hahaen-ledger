# 前端

- `app/pages.json` 将首页调整为首路由，首次使用页移至首页之后但继续注册。
- `app/src/App.vue` 删除小程序启动登录成功和登录重试成功后的首次使用页强制入口，并增加启动完成后的认证页归首页兜底。
- `app/tests/page-flows.test.mjs` 增加首页首路由断言，并确认启动代码不再引用首次使用页。
- H5 认证入口和小程序登录失败重试提示保持不变。
