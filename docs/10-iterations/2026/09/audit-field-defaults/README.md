# 公共字段可空与默认值调整（历史迭代）

> 历史记录：本迭代当时通过 V4 完成规则调整；后续 `flyway-v1-baseline` 已将该最终状态吸收到唯一 V1 基线。本文档保留当时的执行证据，不代表当前 Migration 顺序。

2026-09-06：公共字段仅 created_at 必填且默认当前时间，deleted 可空且默认 0。范围包括六张业务表、Java 自动填充、人工审计 SQL、规范及回归验证。详见本目录 01-10 文档。
