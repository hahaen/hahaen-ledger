# 前端

修改 `app/src/prototype.scss`：

- 底部导航四项改为等宽 flex 子项；
- 移除底部导航整体外围边框；
- 将图标与文字组向下调整 3px，使其在底栏中视觉居中；
- 通过独立字形节点对日历和我的图标做视觉尺寸补偿，使四个图标与首页图标观感一致；
- 清除 uni-app/H5 button 及其 `::after` 伪元素的默认边框、圆角、阴影、内边距和原生 appearance；
- 保留 `.nav-icon` 的选中态薄荷绿背景；
- 保留四项文案、图标、路由、无障碍标签和安全区适配。
