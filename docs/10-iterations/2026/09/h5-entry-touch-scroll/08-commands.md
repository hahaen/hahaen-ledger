# Commands

- `git status --short`：执行；未输出变更（检查开始时工作区干净）。
- `rg -n 'entry-page|entry-content|disableScroll|keyboard|screen-nav|100dvh|overflow' app/src/pages/entry app/src app/pages.json`：执行；确认记账页主体为独立滚动区、数字键盘固定定位，样式在 `app/src/prototype.scss`。
- 自动化测试、类型检查和构建命令：NOT_RUN。
