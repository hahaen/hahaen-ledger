# 设计

复用 `prototype.scss` 中已被账户删除、账单删除和放弃编辑使用的 `asset-create-backdrop`、`asset-create-handle` 与 `account-delete-*` 样式。该组合提供薄荷色半透明遮罩、30px 圆角白色面板、顶部提示条、浅色取消按钮及珊瑚色确认按钮。

页面新增独立的弹层开关。点击设置项仅打开弹层；确认按钮才调用既有 `ledger.logout()`。退出期间，遮罩点击和两个按钮均不再改变状态，避免重复提交或误关闭。退出完成后关闭弹层、重置个人资料和头像并显示既有成功提示。
