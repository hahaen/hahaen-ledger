# `h5-password-eye-toggle`｜登录注册密码显隐图标

## 迭代目的

修复登录页和注册页密码输入框右侧使用圆点字符、缺少直观小眼睛图标的问题。

## 变更范围

认证页面仍由 `app/src/components/AuthPage.vue` 统一渲染；新增 CSS 小眼睛及隐藏状态斜线，不改变认证接口、密码加密、数据库和权限逻辑。

## 验证结论

共享组件静态核对、前端类型检查、H5 生产构建和微信小程序生产构建均应以本次真实命令结果为准；真实后端认证和微信开发者工具人工验收不在本次范围内。
