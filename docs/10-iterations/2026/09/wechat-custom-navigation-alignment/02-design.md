# 02 设计

用统一导航组件读取 `wx.getMenuButtonBoundingClientRect()` 与系统安全区，在普通流中按胶囊 top/height 布置标题行并修正其间距。品牌标题改为水平布局；返回/居中标题导航沿用各页既有按钮和样式。H5 继续使用原 CSS。

不触碰 API、账务、认证、数据结构或资产。CSS 顶部留白以状态栏与安全区较大值为准，避免相加造成重复偏移。
