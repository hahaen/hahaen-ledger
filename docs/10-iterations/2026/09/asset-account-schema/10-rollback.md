# 回滚

## V3 尚未执行时

在任何环境应用前，如设计需要调整，可直接在代码评审阶段修改未发布的 V3 文件，并重新执行静态校验；不得修改 V1 或 V2。

## V3 已在共享环境执行后

不得删除或修改 `V3__create_asset_account_table.sql`，也不得使用手工 `DROP TABLE` 或 `ALTER TABLE` 绕过 Flyway。若确需撤回，应先评估是否已有账户数据，再新增后续版本迁移，按可恢复、可审计的方案处理，并同步更新数据库设计和验证证据。

本轮未执行 V3，因此没有数据库数据回滚动作。
