# 前端

- 新增 `app/src/utils/tapFeedback.ts`，集中处理 H5 捕获式轻触振动和微信轻振动 API。
- `App.vue` 安装 H5 监听，并在微信端使用 App 外层触摸捕获入口。
- `prototype.scss` 为按钮及明确可点击控件增加统一按压反馈和减少动效兼容。
- 日历格、资产账户行、未登录昵称和账户开关补充 `data-tap-feedback`，不改原有点击回调。
- 记账键盘不再保留局部重复振动，统一由 App 捕获入口处理，避免同一次按压双重反馈。
