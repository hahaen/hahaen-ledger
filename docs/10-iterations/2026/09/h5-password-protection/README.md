# `h5-password-protection`｜H5 密码保护

## 迭代目的

本迭代关注 H5 密码从输入、传输到存储的保护链路：浏览器使用服务端公钥加密，服务端解密后校验或生成 BCrypt 哈希，数据库和日志不出现明文密码。

## 应记录的重点

应明确 RSA 算法和密钥来源、私钥配置方式、生产固定密钥要求、密码哈希参数、错误处理、重放/暴露风险和测试边界。前端加密不能替代服务端校验和 TLS。

## 当前档案状态

当前工作区未保留本迭代明细文件；密码安全结论必须以代码审查、单元测试和真实接口脱敏证据为准。
