# 前端

- `App.vue` 启动守卫不再在有效 token 下无条件 `reLaunch` 首页。
- 只有根路径在已有有效会话下才进入首页；业务页和详情页保留原 hash 路由。
- `ledger` 恢复 token 时同时恢复非敏感用户快照，并暴露统一的 `clearSession`。
- `api.ts` 在 401/403 时通知启动层，清理响应式状态和持久化数据，避免界面仍显示为已登录。
