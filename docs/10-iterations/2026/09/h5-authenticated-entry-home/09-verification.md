# 验证

| 验收项 | 状态 | 证据 |
| --- | --- | --- |
| 已认证默认入口进入首页 | PASS（路由单测） | `auth-guard.test.mjs` 断言根路径、登录页、注册页均匹配首页分流，5/5 通过。 |
| 已认证业务页与协议页保留路由 | PASS（路由单测） | 同一测试断言首页和隐私协议页不匹配首页分流，避免改写既有业务与协议路由。 |
| H5 类型检查与生产构建 | PASS | `pnpm run typecheck`、临时注入本地 API 地址后的 `pnpm run build:h5` 均 exit 0，构建输出 `DONE Build complete.`。 |
| 真实关闭页面后重新打开 | NOT_RUN | 需要有效 H5 会话和浏览器操作。 |
| 数据库/Flyway | PASS（范围核对） | 本次仅修改前端启动路由判断、前端测试和文档，无 Schema、Flyway、实体或接口变更。 |
