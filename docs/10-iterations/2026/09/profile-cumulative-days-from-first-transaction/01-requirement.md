# 需求

## 目标

将“我的”页的累计记账天数改为按最早一次记账的时间统计，不使用账号创建时间。

## 范围与验收

- 仅统计当前登录用户的 `transaction_detail` 有效记录（`deleted=0`）。
- 以最早记录的 `occurred_at` 所在自然日到业务当天的自然日差加一计算。
- 最早记账当日为 1 天；没有有效账单为 0 天；异常未来日期不产生负数或虚假的累计值。
- 页面继续显示服务端 `profile.cumulativeDays`，不新增前端自行计算。

退款、账号创建时间、已删除账单和其他用户账单不参与统计。
