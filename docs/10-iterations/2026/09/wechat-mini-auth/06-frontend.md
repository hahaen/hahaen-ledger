# 前端

- `ledger.login()` 在微信小程序编译分支获取新 code，并请求新微信登录接口。
- H5 `AuthPage.vue` 和账号密码 API 不修改。
- 小程序启动时无 token 自动登录；已有 token 先刷新业务数据。
- 微信登录失败时显示可重试状态，不把未登录状态误当作可用会话。
- 认证过期时清理本地会话并允许重新获取微信 code。
- 不在业务页面散落 `wx.*` 调用。
