# 微信小程序主题色跨端兼容

## 目标

修复首页、日历、资产、我的及其复用组件在微信小程序端与 H5 颜色不一致的问题。保留现有视觉色板、页面结构、接口、业务逻辑和 H5 行为。

## 范围

- 共享 SCSS 中的主题色从运行时 CSS 自定义属性引用改为编译期确定的等价颜色值。
- PageHeader 的作用域样式同步使用确定颜色值。
- 增加“源码与微信产物不得残留主题色 `var(--...)` 引用”的静态回归。
- 不修改数据库、Flyway、API、认证、请求参数、业务方法或页面交互逻辑。

## 状态

- PASS：主题色静态回归 54/54、TypeScript、H5 构建、微信小程序构建、WXSS 产物主题色检查和 `git diff --check`。
- BLOCKED / NOT_RUN：当前 UI 自动化面未暴露可控制的微信开发者工具原生窗口，真实小程序页面画面和点击仍未验收。

实现和验证结果记录在 01–10 文件中。构建通过不等于运行时验收通过。
