# H5 退出登录后的路由访问限制

退出 H5 账号后，用户只能访问登录页或注册页；直接输入业务页地址、浏览器前进/后退和历史路由恢复都必须回到登录页。微信小程序不改变原有退出后重新登录并进入首页的行为。

状态：代码、前端回归、类型检查和双端生产构建已通过；真实 H5 浏览器登录态验收仍未执行。

按 `01-requirement.md` 至 `10-rollback.md` 记录本次变更。
