# testing

- 检查首页模板与样式：金额组件在标题后独立渲染，首页作用域规则覆盖金额组件默认 `inline-flex`。
- 独立只读视觉环境返回月份 `2026-09`、日均消费 `86.40`、支出 `2,592` 和收入 `8,500`，布局截图确认金额位于标题下方，支出/收入字段间距适中且未被拉满。
- 静态核对首页底部字段最大宽度为摘要卡可用宽度的一半减去间距，长金额不会覆盖或贴近收入字段。
- 未执行真实认证账务联调；视觉环境仅使用固定只读样例数据。
