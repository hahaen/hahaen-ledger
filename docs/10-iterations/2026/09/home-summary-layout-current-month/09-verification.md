# verification

- PASS：首页日均消费金额已按设计稿移动到标题下方。
- PASS：首页“本月支出”和“本月收入”改为按内容宽度排列，间距不再占满摘要卡。
- PASS：首页月份不是硬编码；`localDateTime()` 在初始化和加载时生成当前 `YYYY-MM`，并用于摘要请求。
- PARTIAL：视觉验收使用独立固定样例数据，未验证真实登录后的数据库账务数据。
- NOT_RUN：微信开发者工具和真实后端账务端到端验收。
