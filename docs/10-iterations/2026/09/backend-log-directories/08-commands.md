# 命令

在 `server/` 目录实际执行：

```powershell
mvn test "-Dtest=LoggingProfileConfigTest"
```

结果：PASS，1 个测试通过，0 failures/errors/skipped。

```powershell
mvn test
```

结果：PASS，41 个测试通过，0 failures/errors/skipped。
