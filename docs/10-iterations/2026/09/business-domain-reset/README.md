# 业务域重置

## 目的

移除当前哈记账业务模型、数据库表、实体后端代码和旧业务设计文档，为后续按表逐张设计留出干净起点。

## 范围

- 删除旧 Flyway 业务表结构和认证扩展迁移。
- 删除 user、book、account、transaction、asset、calendar、home、auth 业务后端代码及其测试。
- 删除旧数据库、API、架构、设计和迭代文档，以及根目录产品需求/原型资料。
- 保留 Spring Boot 启动骨架、通用响应/异常/审计基础设施、文件存储适配器和前端工程。

## 数据库

当前不定义任何业务表。本地开发数据库已在确认目标后完成清空，后续新增业务表应从新的 Flyway 迁移开始。

## 验证

删除完成后执行后端测试和编译，确认工程骨架仍可构建；前端仅保留原有代码，不宣称业务接口可用。

- `server/mvn test`：PASS，2 个通用金额工具测试通过。
- `server/mvn clean package -DskipTests`：PASS，后端打包成功。
- 旧表名、旧实体名和旧业务服务引用扫描：PASS，未发现残留。
- 本地数据库清理：PASS，已核验 `127.0.0.1:3306/haji_dev` 后清空并重新创建，复验表数量为 0。

## 回滚

本次删除内容可通过 Git 工作区恢复。重新设计第一张表时新增正式 Flyway 迁移、对应实体、API、测试和文档。
