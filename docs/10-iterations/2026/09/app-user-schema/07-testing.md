# 测试

状态：`NOT_RUN`。

后续至少补充：

- 空账号用户可用于微信登录。
- H5 账号与密码哈希必须成对存在。
- 重复 H5 账号不能插入。
- 停用用户和逻辑删除用户的 H5 账号仍不能被新用户复用。
- 最近登录 IP 可保存 IPv4 和 IPv6 地址。
- 同一 `provider + open_id` 不能绑定多个用户。
- 同一个用户重复绑定微信身份应保持幂等。
- 已绑定其他用户的微信身份不能自动合并。
- 登录成功/失败日志可分别保存，失败登录可不关联用户。
- 登录日志不能保存密码、Token 或微信 `open_id`。
