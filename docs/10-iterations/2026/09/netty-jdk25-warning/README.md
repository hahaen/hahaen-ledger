# `netty-jdk25-warning`｜Java 25 启动告警处理

## 迭代目的

本迭代用于处理 Java 25 运行时下 Netty 关于 `sun.misc.Unsafe` 的兼容性告警，确保 Maven 启动、直接运行 JAR 和 IDEA 启动入口采用一致且可解释的配置。

## 应记录的重点

应区分“消除噪声告警”和“修复真实运行故障”，记录 `io.netty.noUnsafe=true` 的设置位置、影响范围、启动方式以及升级依赖后的复查要求。不能用屏蔽日志掩盖连接、性能或功能异常。

## 当前档案状态

当前工作区未保留本迭代明细文件；没有启动输出和功能回归证据时，不能把告警处理标记为完整通过。
