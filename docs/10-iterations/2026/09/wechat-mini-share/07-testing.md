# 测试

`app/tests/wechat-share.test.mjs` 静态检查统一标题、首页路径和微信生命周期注册，并直接读取 `pages.json`，逐一验证全部 14 个页面的注册调用，后续新增页面遗漏接入时测试会失败。

本轮实际结果：转发专测 15/15 PASS；全量 `tests/*.test.mjs` 为 77 项中 75 项通过，2 项失败来自工作区既有 `mine` 模板断言与当前 `tap-feedback` 标记不匹配；类型检查 PASS。H5 与微信小程序生产构建均 PASS，微信产物的 14 个页面均引用统一转发模块。真实微信转发仍需微信开发者工具或真机验收。
