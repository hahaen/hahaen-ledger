# Flyway V1 数据库基线收口

- 日期：2026-09-06
- 类型：数据库、Flyway、开发环境重建、文档同步
- 状态：PASS（本次范围）
- 目标：将当前开发阶段完整数据库结构统一收口到唯一 `V1__init_schema.sql`，重建 DEV `haji_dev`，并核对 Schema、Java、Flyway 与文档。

本迭代不修改任何已有配置项、配置结构或配置值；真实凭证不写入本档案。
