# 需求

- 全面排查首页、日历、资产、账户详情、账单详情、交易列表和退款等金额展示。
- `.00` 不展示；如存在有效小数，保留实际两位小数内容，例如 `99.50` 保持为 `99.50`。
- 统一复用公共金额格式化实现，避免页面各自处理 `toFixed` 或 `.00`。
- `null`、`undefined`、字符串和数字输入均不得导致展示报错。
- 仅调整前端展示层，不改变金额接口、数据库和实际计算精度。
