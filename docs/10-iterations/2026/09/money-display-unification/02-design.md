# 设计

- 在 `app/src/utils/money.ts` 中保留 `yuan()` 作为带 `¥` 的公共入口。
- 新增不带货币符号的 `formatYuan()`，供模板已单独渲染 `¥` 的大字号金额使用；两者共享同一个金额文本实现，不复制规则。
- 统一将分值 `number|string|null|undefined` 归一化后按人民币两位小数格式化，仅移除末尾 `.00`。
- 输入框中的编辑值仍保持可提交的字符串，不把展示格式化结果写回后端或改变分值精度。
