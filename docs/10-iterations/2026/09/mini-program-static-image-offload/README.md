# 微信小程序静态图片外置

## 目标

修复微信开发者工具上传报错 `80051`：主包源尺寸超过 2 MB。

## 结果

- 将 8 个非用户私密的界面图片统一改为从已验证的 MinIO 公网对象读取。
- 保留原始图片于 `app/offloaded-static-assets/wx/`，不再由 uni-app 的 `src/static/` 目录自动复制进小程序产物。
- 本地生产构建产物从 3841.50 KB 降为 270.19 KB。

详见本目录 01–10 文档；构建体积符合限制不代表微信开发者工具上传或真机图片加载已经验收。
