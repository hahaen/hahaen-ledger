# 测试

- 前端 TypeScript 类型检查。
- H5 生产构建。
- 微信小程序生产构建。
- 前端回归覆盖退款删除成功后立即移除记录、有效金额回算、刷新失败不回显旧记录。
- 前端回归覆盖详情页首次生命周期不发送空账单 ID 请求。
- 后端回归覆盖退款软删除、账单有效金额回算和支出账户余额恢复。
- 静态核对删除接口返回 0 行更新时会按失败处理，避免误报删除成功。
- 静态核对退款弹层结构、错误提示、默认金额、接口调用和数据库无结构变更。
