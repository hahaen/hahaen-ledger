# 设计

移除账单详情退款流程对通用 `CenterModal` 的依赖，使用与账户新增、删除和还款弹窗相同的自定义弹层结构：半透明遮罩、顶部把手、30px 圆角白色卡片、系统薄荷绿确认按钮和浅色取消按钮。

退款弹窗使用独立的 `refund-*` 样式，避免覆盖通用居中弹窗。内容区采用标题、说明、字段标签、浅薄荷背景输入框、错误提示和底部大按钮的顺序；操作按钮使用安全区底部间距并保持小屏可收缩。

