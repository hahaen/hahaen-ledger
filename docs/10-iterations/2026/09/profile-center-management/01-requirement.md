# 需求

”我的”页点击个人中心后进入独立页面，页头返回和标题遵循关于与帮助页面。页面提供头像、昵称和账号；昵称与账号前端必填，已有账号禁用，账号为空时允许首次设置且必须唯一。底部提供修改密码和保存按钮。

修改密码使用自定义弹层，输入新密码并确认后立即保存密码。保存资料后展示成功弹层，确认后返回“我的”。页面、弹层和按钮必须沿用现有薄荷绿视觉体系。

不新增或修改数据库表、Flyway、账务或文件存储协议。

后续需求补充：未设置密码时，账号下方直接显示首次密码必填输入框，底部仅保留保存按钮；已设置密码时不显示该输入框，并显示修改密码按钮。两处密码输入均复用登录页右侧小眼睛显隐控件。
