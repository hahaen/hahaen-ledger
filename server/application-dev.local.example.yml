# 复制为 application-dev.local.yml 后填写本机开发值。
# 该文件只用于本地开发，真实 AppSecret 禁止提交到 Git。
hahaen:
  wechat:
    app-id: wx-your-mini-program-app-id
    app-secret: replace-with-your-rotated-wechat-app-secret
    login-enabled: true
