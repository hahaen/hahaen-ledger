# 哈记账微信小程序｜完整功能说明

> 版本：v1.0 · 与 `哈记账小程序_原型设计稿/` 对齐
>
> 本文用于产品、UI、前端、Java 后端和测试共同理解第一版功能。它描述“用户能做什么、数据如何变化、异常如何处理和页面如何联动”，不是视觉稿替代品。

## 1. 产品概述

哈记账是一款移动端微信记账小程序，围绕“3 秒记下一笔，10 秒看懂当月”设计。第一版服务单用户、单账本、人民币单币种场景，重点解决三件事：快速记录收支、按日期回看账单、查看资产和账户余额。

产品强调轻量和长期使用，不在第一版引入复杂报表、自动同步、预算或多人协作。用户可以手动记录支出、收入和转账，也可以在资产页管理资金账户和信贷账户，并通过信贷账户详情完成还款。

## 2. 第一版功能边界

### 2.1 已开放功能

| 功能域 | 功能 |
| --- | --- |
| 认证 | H5 账号登录、注册、验证码、登录状态判断和退出登录；微信小程序沿用微信自动登录 |
| 首页 | 月度概览、日均消费、本月支出/收入、日期分组账单、进入账单详情、新增记账 |
| 记账 | 支出、收入、转账；金额计算器；账户、日期时间、备注；新增、编辑、删除 |
| 账单详情 | 查看账户和时间信息；编辑；删除；支出/收入退款；退款记录查看与删除 |
| 日历 | 月份切换、年月选择、日期选择、当天收支小结、当天账单列表 |
| 资产 | 净资产、总资产、总负债；资金账户和信贷账户分组；展开/收起 |
| 资金账户 | 新增、查看详情、流水筛选、编辑名称/余额/净资产开关、删除 |
| 信贷账户 | 新增、查看详情、流水筛选、编辑额度/欠款/净资产开关、还款、删除 |
| 个人与帮助 | 头像授权状态、累计记账天数、FAQ、隐私说明、关于版本信息 |
| 首次使用 | 开始记账、稍后设置账户 |

### 2.2 占位或暂未开放

- 统计：仅保留后续扩展位置，不作为首版底部导航入口。
- 数据导出/删除：显示“暂未开放”，不提供真实操作。
- 反馈建议：只作为入口预留或 Toast 反馈，不提交真实表单。
- 头像相册选择和文件上传：当前原型只展示微信头像授权的未授权、授权中、成功状态。

### 2.3 明确不属于第一版

账单分类选择和分类管理、账单凭证、拍照、OCR、自动入账、周期记账、预算、多人共享账本、银行卡/支付平台自动同步、投资收益、多币种、拆分账单、分期和手续费。

## 3. 信息架构与页面关系

```text
打开应用
  ├─ H5：判断登录状态
  │    ├─ 未登录 ─ 登录页 ─ 注册页 ─ 返回登录
  │    └─ 有效登录 ─ 首页
  └─ 微信小程序：微信自动登录/恢复会话 ─ 首页
       └─ 退出登录 ─ H5 登录页（微信小程序下次打开仍走微信自动登录）

首次使用
  └─ 开始记账 / 稍后设置账户
       └─ 首页
          ├─ 新增记账 ───────────────┐
          ├─ 账单详情 ─ 编辑/退款/删除 │
          └─ 底部导航                 │
             ├─ 日历 ─ 当天账单 ──────┘
             ├─ 资产
             │  ├─ 资金账户详情 ─ 流水筛选/编辑/删除
             │  └─ 信贷账户详情 ─ 流水筛选/编辑/还款/删除
             └─ 我的 ─ 关于与帮助 ─ FAQ/隐私/暂未开放
```

首版底部导航固定为：首页、日历、资产、我的。新增记账、账单详情和账户详情属于二级页面，不能出现在底部导航。

认证页面不属于底部导航。H5 未登录时登录页是默认入口；登录页和注册页采用与业务页面一致的 V1 视觉令牌。微信小程序正常情况下不展示账号密码登录页。

## 4. 用户流程说明

### 4.0 登录、注册和多端认证

#### H5

1. 打开 H5 后先判断本地会话是否存在且仍有效。
2. 未登录或会话失效进入登录页；登录页包含账号、密码、图形验证码输入框、验证码图片、登录按钮和注册入口。
3. 图形验证码默认展示，点击验证码图片刷新并清空验证码输入；验证码错误时保留账号和密码，提示重新输入或点击验证码图片刷新。
4. 账号字段使用产品最终确定的 H5 账号标识；原型只使用“输入账号”占位，不预设手机号、邮箱或用户名之一。密码不在页面明文展示，也不要求昵称、头像等注册资料。
5. 登录失败显示“账号或密码错误，请检查后重试”，登录按钮在必填项不完整或提交中禁用；成功后进入首页。
6. 注册页只填写账号、密码、验证码。注册成功后回到登录页并保留账号，或由服务端建立有效会话后直接进入首页；本 V1 原型采用“注册成功，返回登录”的可核验路径。
7. 用户在“我的”页退出登录后清理本地会话并回到登录页；业务接口返回 401/403 时视为登录状态失效，同样回到登录页并允许重试。

#### 微信小程序

1. 打开小程序后继续执行现有微信登录/自动登录逻辑：恢复有效会话，或调用 `wx.login` 获取 code 后由服务端完成登录。
2. 登录成功直接进入首页，正常情况下不先展示账号密码登录页。
3. 微信登录失败保留当前自动登录重试入口；不得把 H5 账号密码表单插入微信小程序首屏。

多端差异只改变认证入口，不改变首页、账本、账户和账单业务结构。账号密码认证属于 H5 复用能力设计；当前仓库后端已实现的是微信登录接口，H5 账号/密码/验证码接口需后续按安全方案补齐后再联调。

### 4.1 首次使用

1. 用户进入首次使用页，看到品牌、产品承诺和自然插画。
2. 点击“开始记账”，进入首页并出现欢迎提示。
3. 点击“稍后设置账户”，跳过账户设置直接进入首页。
4. 不强制头像授权，不强制通知授权，不强制一次性创建账户。

首次使用页的核心目标是降低第一次操作门槛。账户和头像都可以在后续流程中处理。

### 4.2 新增支出/收入

1. 用户点击首页或其他业务页的 `+`，进入全屏新增记账页。
2. 默认类型为支出；默认账户优先使用上一次保存记账时使用的账户。
3. 用户用金额键盘输入金额或简单算式。
4. 用户选择资金账户，确认日期与时间，可填写备注。
5. 点击“确定/保存”，前端校验金额和字段，后端保存账单并更新账户余额。
6. 保存成功后回到来源页或详情页，显示轻量成功提示。

收入和支出共用页面结构，只使用颜色、类型标签和金额正负号区分语义。收入使用柔和红色，支出使用青绿色。

### 4.3 新增转账

1. 在新增记账页切换到“转账”。
2. 页面将“资金账户”替换为“转出账户”和“转入账户”。
3. 两个账户必须来自可用资金账户，且不能相同。
4. 保存后转出账户减少、转入账户增加。
5. 转账不进入收入、支出和本月结余，但在首页、日历和账户流水中可查看。

### 4.4 查看和编辑账单

1. 首页、日历、资金账户流水、信贷账户流水中的每条账单均可点击。
2. 进入账单详情，查看类型、金额、账户、时间、备注和记录编号。
3. 点击“编辑”进入回填后的全屏编辑页。
4. 保存编辑后，刷新相关账户余额、首页分组汇总、日历小结和详情内容。
5. 编辑过程中返回，如果内容已改变，显示“继续编辑/放弃更改”。

### 4.5 退款

1. 只有支出和收入详情显示“退款”按钮，转账和还款不显示。
2. 用户输入本次退款金额，最大不能超过剩余可退款金额。
3. 支持部分退款和多次退款；累计退款等于原金额时，状态变为“已退款”。
4. 退款成功后，详情显示累计退款和退款明细，并在账单列表增加“退”标识。
5. 删除某条退款记录必须二次确认；删除后恢复对应有效收支金额。

退款不是一笔新的普通收入或支出，而是原账单的冲销记录。正式实现时需保证退款操作幂等。

### 4.6 管理资金账户

1. 用户进入资产页，点击右下角 `+`。
2. 选择“资金账户”，填写账户名称、余额和是否计入净资产。
3. 保存后账户出现在资金账户分组。
4. 点击账户进入账户详情，可查看流水并按类型筛选。
5. 编辑账户时可修改名称、余额和净资产开关；名称不能与其他账户重复。
6. 余额发生变化时，询问是否加入“账户余额补齐”流水：
   - 加入流水：生成一条收入或支出校准记录。
   - 不加入流水：只更新余额，不增加普通账单。
7. 删除账户前二次确认；正式实现对有历史流水的账户使用停用，历史账单继续保留。

### 4.7 管理信贷账户与还款

1. 用户在资产页点击 `+`，选择“信贷账户”。
2. 填写账户名称、总额度、当前欠款、是否计入净资产。
3. 欠款不能大于总额度，名称不能和其他账户重复。
4. 进入信贷账户详情，查看当前欠款、总额度、可用额度和流水。
5. 点击“还款”，选择一个资金账户并填写还款金额。
6. 金额必须大于 0、不超过当前欠款，且资金账户余额充足。
7. 还款成功后：资金账户余额减少、信贷账户欠款减少、可用额度增加，并写入还款流水。
8. 还款流水可编辑或删除；服务端必须先撤销旧的账户影响，再应用新的影响，避免重复扣款。

## 5. 页面功能说明

### 5.1 首页

#### 页面内容

- 品牌图标和辅助文案“记录每一笔，让生活更清晰”。
- 月度概览卡：当前年月、日均消费、本月支出、本月收入。
- “最近记账”标题。
- 按日期倒序分组的账单列表。
- 固定悬浮新增按钮和底部导航。

#### 默认数据

原型提供可读示例：2026 年 9 月、日均消费 `¥86.40`、本月支出 `¥2,592.00`、本月收入 `¥8,500.00`，并展示收入、支出、转账账单。

正式实现必须以服务端计算结果为准，不得把原型示例金额写死。

#### 空、加载和错误

- 无账单：显示“暂无记账记录”和“添加第一笔记账”。
- 加载：保留摘要卡和列表结构，使用骨架或按钮内加载。
- 网络错误：说明“暂时无法加载账单”，提供“重试”；如已有缓存，保留已加载数据。

### 5.2 新增/编辑记账

#### 字段

| 字段 | 支出 | 收入 | 转账 | 规则 |
| --- | --- | --- | --- | --- |
| 金额 | 必填 | 必填 | 必填 | `0.01`～`999999999.99` |
| 资金账户 | 必填 | 必填 | 不使用 | 只选择资金账户 |
| 转出账户 | 不使用 | 不使用 | 必填 | 不能与转入账户相同 |
| 转入账户 | 不使用 | 不使用 | 必填 | 不能与转出账户相同 |
| 日期与时间 | 必填 | 必填 | 必填 | 默认设备本地当前时间 |
| 备注 | 可选 | 可选 | 可选 | 最多 100 字 |

当前原型不提供分类和账单凭证字段。`餐饮`、`工资`等仅是示例数据的来源标签，不应在表单中新增分类选择器。

#### 计算器规则

- 支持数字、一个小数点、加、减、乘、除。
- 支持连续简单运算，按标准运算优先级计算。
- 支持清除、退格、等号。
- 小数最多两位，金额显示统一保留两位或按界面格式化。
- 除数为零显示“除数不能为 0，请重新输入”。
- 算式不完整或非法显示“算式有误，请检查后重试”。
- 保存前将完整算式转为最终结果，编辑时回填结果而非原始算式。

#### 保存校验

- 金额为空或小于等于 0：提示“请输入大于 0 的金额”。
- 金额超过上限：提示“金额不能超过 ¥999,999,999.99”。
- 转出和转入相同：提示“转出账户和转入账户不能相同”。
- 保存中禁止重复点击。

### 5.3 账单详情

详情页的金额、类型、状态使用大面积轻卡片突出；信息区按“账户、时间、备注”展示；底部固定操作区。

操作按钮：

- 支出/收入：编辑、退款、删除。
- 转账/还款：编辑、删除。
- 删除账单：二次确认并说明会同步刷新首页、日历和相关账户余额。

### 5.4 日历

- 支持上月、当前月和年月选择。
- 有记录日期显示低对比度类型圆点。
- 选中日期显示青绿色状态；今天有独立标识。
- 选中日显示当日支出、当日收入、当日结余。
- 转账和还款显示在列表，但不进入收支结余。
- 无记录显示“这一天还没有记账”。

月份切换后要重新加载对应月份的日期标记和账单列表；选择某一天只刷新日汇总和当天列表。

### 5.5 资产概览

资产卡展示：

- 净资产：总资产 − 总负债。
- 总资产：计入净资产的资金账户余额之和。
- 总负债：计入净资产的信贷账户欠款之和。

信贷账户和资金账户均支持分组收起/展开。空分组显示“暂无信贷账户”或“暂无资金账户”。整个资产页没有复杂表格和图表。

### 5.6 资金账户详情

- 账户名称、资金账户标识、是否计入净资产、当前余额。
- 账户流水列表按日期分组。
- 类型筛选：全部、支出、收入、转账、还款。
- 空流水：显示“这个账户的收支会在这里呈现”。
- 操作：编辑账户、删除账户。

### 5.7 信贷账户详情

- 账户名称、信贷账户标识、是否计入净资产。
- 当前欠款、总额度、可用额度。
- 账户流水和类型筛选。
- 操作：编辑账户、还款、删除账户。
- 欠款为 0 时，还款按钮显示“已还清”并禁用。

### 5.8 我的和关于与帮助

我的页包括默认头像、账本主人、头像授权状态、累计记账 28 日示例和关于与帮助入口。

头像状态：

- 未授权：显示“头像未授权 · 可继续使用”和“去授权”。
- 授权中：显示“正在获取微信头像…”，按钮禁用。
- 成功：显示“头像已授权 · 数据随时可用”和“更换头像”。

关于与帮助页包括快速开始卡片、FAQ、隐私说明、数据导出/删除暂未开放、反馈建议预留和原型版本号。

## 6. 数据模型建议

### 6.1 User 用户

| 字段 | 说明 |
| --- | --- |
| `id` | 用户唯一标识 |
| `openId` | 微信登录标识，服务端保存；仅微信小程序认证使用 |
| `nickname` | 显示名称 |
| `avatarFileId` | 可选头像文件元数据 ID |
| `createdAt` / `updatedAt` | 创建和更新时间 |

H5 账号、密码摘要、验证码挑战和会话字段不在本次原型改动中落库。正式实现前需单独设计账号唯一性、密码哈希、验证码有效期/限流、会话失效和审计字段，不得明文保存密码或验证码。

### 6.2 Book 账本

第一版每个用户默认一个账本。

| 字段 | 说明 |
| --- | --- |
| `id` | 账本 ID |
| `userId` | 所属用户 |
| `name` | 账本名称 |
| `currency` | 固定 `CNY` |
| `timezone` | 用户统计时区 |
| `status` | 正常/停用 |

### 6.3 Account 账户

```text
id, bookId, name, kind, balance, creditLimit, includedInNetAsset,
status, initialBalance, createdAt, updatedAt
```

- `kind=FUND`：资金账户，使用余额表达。
- `kind=CREDIT`：信贷账户，使用当前欠款和总额度表达。
- 账户名称在同一账本内唯一。
- 停用账户不能出现在新增账单的选择器中，但历史关联保留。

### 6.4 Transaction 账单

```text
id, bookId, type, amountCents, accountId, fromAccountId,
toAccountId, occurredAt, note, status, createdAt, updatedAt
```

- `type`：`EXPENSE`、`INCOME`、`TRANSFER`、`REPAYMENT`。
- 支出/收入使用 `accountId`。
- 转账使用 `fromAccountId` 和 `toAccountId`。
- 还款使用资金账户和信贷账户的来源/目标关系。
- 正式实现可保留内部 `category` 字段，但第一版不向用户暴露分类 UI。
- `status` 至少包含正常、已删除，正式实现建议记录删除原因和操作者。

### 6.5 RefundRecord 退款记录

```text
id, transactionId, amountCents, refundedAt, status, createdAt
```

约束：退款记录只能挂在支出或收入上；退款累计金额不能超过原账单金额；删除退款必须可追溯。

## 7. 业务计算规则

### 7.1 账户余额

资金账户余额建议按“期初余额 + 流水影响 + 可选校准记录”计算。转账、收入、支出、还款对账户的影响如下：

| 业务 | 资金账户 | 信贷账户 |
| --- | --- | --- |
| 支出 | 减少 | 不允许直接记入信贷账户，正式规则可按产品实现扩展 |
| 收入 | 增加 | 不允许直接记入信贷账户 |
| 转账 | 转出减少、转入增加 | 不参与 |
| 还款 | 还款资金账户减少 | 当前欠款减少 |

退款对原支出/收入产生反向影响，有效金额为：

```text
有效金额 = 原账单金额 - 已确认退款金额
```

### 7.2 月度统计

- 本月支出：当前账本、当前用户本地自然月内，状态正常的支出有效金额之和。
- 本月收入：同口径下的收入有效金额之和。
- 本月结余：本月收入 − 本月支出。
- 日均消费：本月已过去天数内的支出有效金额之和 ÷ 已过去天数。
- 未来日期可补记，但未来日期不进入日均消费分母。
- 转账、还款、账户余额补齐记录不进入月度收支统计；若产品将补齐记录作为收入/支出展示，需用内部标识排除统计。

### 7.3 资产统计

```text
总资产 = Σ（计入净资产的资金账户余额）
总负债 = Σ（计入净资产的信贷账户当前欠款）
净资产 = 总资产 - 总负债
```

所有金额使用整数分或 Decimal 运算，禁止用 JavaScript/Java 浮点数直接做账务加减和比较。

## 8. 接口职责建议

接口名称可按 Java 项目规范调整，但职责应保持一致：

当前仓库已实现的微信登录入口为 `POST /api/app/auth/login`（请求体为微信 `code`）；下表中带“规划”的账号密码接口仅用于本次 H5 产品/原型对齐，尚未完成后端联调。

| 接口 | 用途 |
| --- | --- |
| `POST /api/app/auth/password/login`（规划） | H5 账号、密码、验证码登录并返回会话 |
| `POST /api/app/auth/password/register`（规划） | H5 账号、密码、验证码注册 |
| `GET /api/app/auth/password/captcha`（规划） | 获取 H5 图形验证码图片和挑战标识 |
| `POST /api/app/auth/login` | 微信登录并返回会话（当前仓库已实现） |
| `GET /api/books/current` | 获取当前账本和用户本地时区 |
| `GET /api/home/summary?month=YYYY-MM` | 获取日均消费、月度收支和最近账单 |
| `GET /api/transactions` | 按日期、月份、账户、类型分页查询账单 |
| `POST /api/transactions` | 创建支出、收入、转账或还款 |
| `GET /api/transactions/{id}` | 获取账单详情及退款记录 |
| `PUT /api/transactions/{id}` | 编辑账单 |
| `DELETE /api/transactions/{id}` | 软删除账单 |
| `POST /api/transactions/{id}/refunds` | 创建退款记录，要求幂等 |
| `DELETE /api/refunds/{id}` | 删除退款记录并恢复有效金额 |
| `GET /api/calendar?year=YYYY&month=MM` | 获取月历标记和选中日数据 |
| `GET /api/accounts` | 获取资金/信贷账户列表和汇总 |
| `POST /api/accounts` | 新增账户 |
| `GET /api/accounts/{id}` | 获取账户详情和流水 |
| `PUT /api/accounts/{id}` | 编辑账户或余额校准 |
| `POST /api/accounts/{id}/repayments` | 从资金账户发起还款 |
| `DELETE /api/accounts/{id}` | 停用账户或执行可删除账户的删除 |
| `POST /api/profile/avatar-auth` | 保存头像授权结果，拒绝不阻塞业务 |

所有写接口建议支持幂等键、服务端金额校验、用户/账本归属校验和统一错误码。前端不能依据本地状态直接认定账务成功，必须以服务端成功响应为准。

### 8.1 文件服务预留（不属于当前 UI）

如果后续开放头像文件或账单凭证，文件链路由 Java 后端统一鉴权，MinIO 只负责对象存储。建议接口职责如下：

| 接口 | 用途 |
| --- | --- |
| `POST /api/files/upload-url` | 按业务类型、文件名、MIME、大小和幂等 Key 获取短时效上传授权 |
| `POST /api/files/{fileId}/complete` | 后端确认对象存在并更新上传状态 |
| `GET /api/files/{fileId}/view-url` | 校验用户和业务归属后返回短时效预览地址 |
| `DELETE /api/files/{fileId}` | 软删除元数据，并异步清理 MinIO 对象 |
| `POST /api/transactions/{id}/attachments` | 将已确认文件关联到账单 |

文件状态至少包含 `UPLOADING`、`READY`、`FAILED`、`DELETING`、`DELETED`。首版不在记账页面展示凭证入口；未来开放时也不能把上传失败当成账单保存失败。头像和凭证属于用户隐私数据，使用私有桶、后端鉴权和短时效 URL，且 Access Key、Secret Key、桶名和内部 Endpoint 不得出现在小程序代码或设计稿中。

## 9. 错误、加载与反馈

### 9.1 通用错误原则

错误文案要回答三件事：发生了什么、数据是否已保存、下一步做什么。示例：

| 场景 | 建议文案 |
| --- | --- |
| 金额为空 | 请输入大于 0 的金额 |
| 金额超限 | 金额不能超过 ¥999,999,999.99 |
| 除数为零 | 除数不能为 0，请重新输入 |
| 算式错误 | 算式有误，请检查后重试 |
| 转账账户相同 | 转出账户和转入账户不能相同 |
| 账户名称重复 | 账户名称已存在，请换一个名称 |
| 欠款超过额度 | 欠款不能超过总额度 |
| 还款金额超限 | 还款金额不能超过当前欠款 |
| 余额不足 | 还款账户余额不足，请更换账户或调整金额 |
| 网络失败 | 暂时无法加载，请重试 |
| 重复提交 | 正在保存，请稍候 |

### 9.2 状态要求

- 默认：展示正常数据和可用操作。
- 加载：保留结构，按钮内显示加载并禁用重复操作。
- 空：展示原因和一个直接相关的主操作。
- 错误：保留已有数据，给出重试；不能把网络错误误显示为“暂无数据”。
- 成功：Toast 或轻提示，明确说明是否已刷新余额。
- 删除：明确影响和不可逆性，正式实现优先软删除/停用。

## 10. 前端状态与组件建议

### 10.1 核心路由状态

```text
first-use
home
entry
transaction-detail
calendar
assets
fund-detail
credit-detail
repayment-edit
mine
about
```

### 10.2 可复用组件

- `PageHeader`：品牌/标题/返回。
- `BottomNav`：四项底部导航。
- `SummaryCard`：月度概览和资产概览。
- `TransactionItem`：类型图标、标签、账户、金额和箭头。
- `TypeSwitcher`：支出/收入/转账。
- `AmountKeypad`：金额和运算键盘。
- `AccountPicker`：资金账户、转出账户、转入账户、还款账户选择。
- `DateMonthPicker`：月份和年月选择。
- `ConfirmSheet`：删除、放弃编辑、删除退款等确认。
- `AccountSection`：资产分组展开/收起。
- `AccountDetail`：账户余额、额度、流水和筛选。
- `Toast`：保存成功、预留入口、授权结果等轻反馈。
- `EmptyState`、`LoadingState`、`ErrorState`：统一页面状态。

## 11. 原型与正式实现的注意事项

当前 HTML 原型是交互和视觉参考，部分行为是演示性质：

1. 首页概览金额和账户示例数据用于展示布局，正式版本必须改为接口返回。
2. 原型的保存、授权和部分反馈使用本地状态/延时模拟，正式实现要替换为真实接口和失败重试。
3. 原型中的日期与备注弹窗主要展示交互样式，正式实现必须把用户选择结果写回表单状态。
4. 原型账单中存在示例退款、还款和余额补齐记录，正式实现应通过明确的后端业务记录生成。
5. 账户删除在原型中使用本地隐藏效果，正式版本应按“有流水停用、无流水可删除”的规则实现。
6. 原型没有账单分类和凭证入口，正式前端不能根据旧版需求自行补回这些字段。

## 12. 测试验收清单

### 12.0 认证与多端路由

- [ ] H5 无有效会话进入登录页，有效会话直接进入首页。
- [ ] 登录页包含账号、密码、图形验证码输入框、可点击验证码图片、登录按钮和注册入口。
- [ ] 注册页只包含账号、密码、验证码、注册按钮和返回登录入口，不出现昵称/头像必填项。
- [ ] 图形验证码默认展示、点击图片刷新、验证码错误和必填项不完整时的状态正确。
- [ ] 登录失败和图形验证码错误有明确反馈；登录提交中按钮禁用；登录成功进入首页。
- [ ] 注册成功按当前 V1 路径回到登录页；退出登录和 401/403 失效均回到登录页。
- [ ] 微信小程序继续自动登录并直接进入首页，不展示账号密码登录页。

### 12.1 导航与页面

- [ ] 底部只有首页、日历、资产、我的四项。
- [ ] 首次使用的两个按钮都能进入首页。
- [ ] 每条账单都能从首页、日历或账户流水进入详情。
- [ ] 详情、账户详情和关于帮助均能正确返回来源页。

### 12.2 记账与计算器

- [ ] 支出、收入、转账切换正确。
- [ ] 空金额、负数、超大金额、小数位超限均有提示。
- [ ] `1 + 2 × 3` 按标准优先级得到 `7`。
- [ ] 除数为零、连续运算符、退格和清除状态正确。
- [ ] 转账不能选择相同账户。
- [ ] 保存中不能重复提交，成功后刷新余额和列表。
- [ ] 编辑离开有未保存确认。

### 12.3 详情与退款

- [ ] 支出/收入可部分退款和多次退款。
- [ ] 累计退款不能超过原金额。
- [ ] 全额退款后按钮禁用并显示已退款。
- [ ] 删除退款记录后有效金额和汇总恢复。
- [ ] 删除账单有二次确认，并刷新关联页面。

### 12.4 日历与资产

- [ ] 月份切换正确刷新记录标记。
- [ ] 日历当日支出、收入、结余不包含转账和还款。
- [ ] 净资产、总资产、总负债口径正确。
- [ ] 资金账户和信贷账户分组可收起/展开。
- [ ] 欠款不超过额度，余额不足不能还款。
- [ ] 还款生成流水且可编辑/删除，不重复扣款。
- [ ] 有历史流水的账户停用后历史账单仍可查看。

### 12.5 适配与可访问性

- [ ] 320px、375px、414px 均无横向滚动。
- [ ] 悬浮 `+` 不遮挡最后一条账单，底部导航避开安全区。
- [ ] 所有图标按钮有无障碍名称。
- [ ] 收入、支出、负债同时有文字或符号，不能只依赖颜色。
- [ ] 动效支持减少动效偏好。

## 13. 后续版本候选

只有在第一版账单、账户和资产口径稳定后，才评估以下能力：账单分类、凭证图片、头像文件上传、数据导出与删除、统计图表、预算、周期记账、多人共享、多币种和支付平台同步。每项能力都需要单独补充权限、数据模型、异常流程和验收标准，不应直接从“预留”变成可用入口。
